# 顶栏与导航图标精修 · 2026-09-18

最终 `npm run verify:full` exit 0：20/20 文件、200/200 用例、lint、生产与文档构建、结构预算、diff 通过。日志：`/tmp/pinax-chrome-verify-final.log`。

范围：公共工作台标签、首页导航、活动导航、写作工具栏与缩放后的章节入口；资料页由另一 agent 负责。

- 复用现有 Lucide 矢量系统，为写作、设定、资料、大纲、角色、批注、推演、现场、助手、历史、双栏、画布和协作登记明确语义；未知名称显示缺失图标，不再伪装成星光功能。
- 标签明确宽度，修正兄弟关闭按钮改造后的分隔线选择器；右上操作区不被标签挤压。
- 工具图标 19px、首页图标 18px，保留文字名称；写作工具支持可见键盘焦点。
- 手机工具栏宽度铺满并保留安全区；200% 缩放时章节入口至少 96px，防止挤压裁切。

视觉证据：`/tmp/pinax-chrome-20260918/`，1440/900/390 写作与首页明暗截图。已查看桌面、手机与首页暗色原图。写作三尺寸审计最终 exit 0（0 console / a11y / scenario failures）；首次审计发现 200% 章节按钮裁切，修复后通过。真实 pointer click 关闭活动标签并返回首页通过。

按 ui-style-check 保留中性工作台、内容主导与现有路由交互。验收仅覆盖本轮公共导航切片，产品其他业务面板仍需后续细化；不需要新规则。
