# 条目管理优化 · 2026-09-18

当前 main 工作区上的增量修改，保留首页/资料页及其他既有 WIP。沿用上轮 Novelcrafter 资料目录参考和项目 UI 规范，不改世界书存储、注入算法或模型调用。

- 默认进入条目目录；基础、导入导出、分组仍保留独立入口。
- 检索与操作使用一致控件，标题/计数/说明分层，新增与 AI 为次级动作。
- 批量工具仅有选择时出现；目录保留全选入口，条目选择使用原生按钮，支持键盘。
- 编辑区将名称/类型、两种触发词成组；移除横线底纹，正文独立阅读面。
- 保存靠近标题且为唯一编辑主动作；删除降为底部次级危险动作，原确认保留。
- 高级引用设置折叠，所有参数继续可访问；角色声口编辑保留。
- 桌面目录与正文分栏，手机目录限高并顺序排列编辑区。无装饰性新素材。

浏览器脚本 `scripts/entries-polish-check.mjs` exit 0：三尺寸、暗色、搜索、键盘选择、批量工具显示/收起、保存刷新、高级参数均通过。截图 `/tmp/pinax-entries-polish/`，已查看桌面、手机与暗色。共享设定联动 `settings-linkage-check` exit 0，20/20。

`npm run verify:full` exit 0：20/20 files / 200/200 tests / build OK / diff clean / docs OK；lint 0 warnings、结构预算通过。日志 `/tmp/pinax-entries-verify-final.log`。

用户视觉确认仍待定。未提交、推送或操作用户浏览器数据。
