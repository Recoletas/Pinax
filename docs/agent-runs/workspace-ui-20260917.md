# UI 首片与 ABC 续作交接

2026-09-17，基线 `5e14e39`，本地 main 未提交。证据等级：已实施/本地组合验证，视觉未获用户确认。用户另行启动后端 agent；未启动 O 或新 worker。

## 实际变更

- 交接任务书 `docs/plan/abc-remaining-20260917.md`：A/B/C 各12项，已完成基础不重做；新基线、UI写锁、角色知识/备份共享依赖、反例及按线回执要求。
- 首页标签复用现有 Pinax PNG；标签桌面36px、整栏40px，手机保留40px标签；去活动页下横线。工具名放在书名前，避免多个同书标签截断后无法区分。
- Authoring 两个准备表单通过原 Teleport 实例移动到检查器 host，切换助手再回来保留输入。正文留简短位置提示，试稿/采用/撤销仍走原 owner。既有双栏模式卸载检查器时保留 inline fallback，不丢表单。窄屏仍顺序展开，点击提示滚到推演区域，保留“正文”回程；没有强行塞三栏。
- 中性色背景、紧凑工具栏、Lucide固定/关闭图标、助手输入容器与建议层级、记忆入口收进助手标题栏；现场概览层级整理，单个未决事件也展示。
- 地图改为填满 shell 剩余高度、去画布外层套框，目录宽度对齐文档；只改UI，不改生成器/相机。文档继续原路由和Markdown，移动目录选章后关闭。创作规则保留原字段存储，列表连续排版、去禁忌emoji，聚焦只突出输入区。

## 验证与取证

端口5307、新建隔离Playwright context；用户5173服务未改。模型禁外发或使用既有确定性 mock。

- `workspace-refinement-check.mjs`：1440/900/390亮色与1440暗色；产品图加载、标签高度/底线、右栏归属、换工具草稿保留、窄屏定位/主按钮可见、长规则、现场/地图/文档与手机目录，无页面横向溢出/运行错误。新断言并入既有 uiControlContract 用例，不扩Vitest预算。
- `rehearsal-panel-check.mjs`：本轮选1440/900/390，155/155，exit 0（不是完整六宽304项）。
- `settings-linkage-check.mjs`：20/20，exit 0。
- `f1-boundary-check.mjs`：21/21，exit 0，含试稿结构调整零正式写入。初跑失败：旧脚本找“2个单元将原子纳入”和未展开的结构按钮；HEAD本来已使用“调整结构”details。按现行UI更新脚本，并增加默认折叠断言，而非改生产行为迁就旧脚本。
- `npm run verify:full`：最终门禁 exit 0；20/20文件、200/200用例、lint 0 warnings、Vite/VitePress、架构与diff通过。终端日志 `/tmp/pinax-ui-20260917-final-verify.log`。

实际查看了 `/tmp/pinax-workspace-refinement/` 的桌面推演、助手、规则、地图、文档、手机推演和暗色推演原视口截图。窄屏定位的首次测试错误要求header必须小于180px，实测短面板到容器末尾时header位于341px而输入与主按钮都完整可见；改成“header进入上半屏且主按钮在工具带上方”的可操作性断言。截图没有预滚美化；窄屏composer截图记录的是用户点击定位后的实际状态。

## 视觉判断与保留边界

保留：原书架封面/品牌、正文阅读、真实来源、保存与采用语义。优先修复：主次区域抢占、标签重复/过大、不同工具的容器语言。当前字级/分隔/焦点更一致，正文是第一阅读对象，但不是全产品的最终设计验收。

尚未覆盖：所有地图已生成数据状态的视觉取证、真实中文输入法/实体设备、真实模型质量、完整跑团页与漫画页的视觉重做。规则编辑能力没有升级为全新富文本或数据库方案。后续按用户截图迭代，不把本轮中性化当成“Apple所有交互已复刻”。

## 侧栏与控件续片

按用户“你来”独立实施，没有启动子 agent。采用仓库 ui-style-check 的成熟产品调研与截图门禁；参考链接及范围见交互合同续节。不增加依赖，复用 Lucide、原生按钮/details 与现有 control 层。

- 新增 `workspace-navigation.css`：显式 opt-in，主导航36px/树32px、主字14px/辅助12px、桌面240px基准；当前项失焦中性底色、栏内聚焦淡强调色、独立键盘轮廓。首页/正文及构思/设定/文档/地图/素材/漫画接入；窄屏保留原断点。缩略图与摘要目录仍有自己的高度。
- 现有 control 层区分主/次/普通/图标/开关/危险动作，补全按下和 aria-disabled 排除，统一6px圆角、14px文字与120–150ms轻量反馈；重置仅匹配交互元素，不再把同名字段容器当按钮。编辑工具栏、助手头部、设定和地图主动作接入。不是已迁移产品所有历史弹窗。
- 首页快捷菜单补焦点/按下反馈，Escape返回触发器；不另造菜单库。地图行内操作预留空间，悬停不挤标题，键盘聚焦和触屏均可用。章节行支持 Enter/Space。
- 地图重命名 Escape 明确取消并归还焦点，commit 增加编辑态防重入，避免退出编辑时的 blur 重复写入。浏览器实际填入新名字后取消，确认旧名未变。

本续片重新运行（非沿用上节数字）：

- `/tmp/pinax-navigation-smoke.log`：`workspace-refinement-check.mjs` exit 0，1440/900/390亮色（390启用触屏）及1440暗色；桌面七类侧栏宽度/字号/底色、聚焦选中反馈、hover几何不变、菜单Escape、禁用主按钮、地图键盘/取消重命名、减少动态效果、推演草稿/定位/长规则、素材和真实新建空白漫画页。零页面运行错误/横向溢出。
- `/tmp/pinax-navigation-rehearsal.log`：1440/900/390 推演155/155，exit 0。
- `/tmp/pinax-navigation-linkage.log`：设定联动20/20，exit 0。
- `/tmp/pinax-navigation-f1.log`：试稿边界21/21，exit 0。
- 最终全量验证：`npm run verify:full` exit 0，20/20 文件、200/200 用例、lint 0 warnings、Vite/VitePress、架构与 diff 通过；日志 `/tmp/pinax-navigation-verify.log`，未以 focused tests 代替门禁。

已实际查看原视口：首页、桌面/手机/暗色推演、900规则、桌面地图、手机文档、素材和已建页漫画。观察：左侧目录尺度与颜色已收口；书架保留品牌层级，漫画保留可操作缩略图而不是假封面；长规则在900px换行而不内滚。首页截图记录了菜单Escape后的焦点轮廓，手机推演记录真实点击定位后的卷动状态，没有为美观重置。

边界：未改数据库/生成协议，未调用真实模型，未操作用户5173服务或真实浏览器存储，未提交推送；仍需用户视觉确认。跑团等独立复杂业务页没有在本续片整体重做。不需要新 skill 规则，本轮是在执行已有规范。

## 14:30 用户截图纠偏

已实施，视觉待用户确认：

- 设定目录延伸到视口底部，长内容使用原 Autosize；聚焦不再同时绘制字段下线和分区高亮线。
- 推演去除起点提示与重复标题文案，默认一份输入；类型和额外约束折叠在“推演选项”。原目标冻结、人物校验、提交合同未变。助手问题示例按需展开、范围用选择器；批注/改写输入接入同一边框与焦点样式。现场默认底部短状态，展开仍可编辑与跳转，引用失效在折叠状态也可见。
- 删除顶部总数与旧导航按钮，窄屏保留全部标签横滚。右上角接回现有夜间切换，设置恢复外观与缩放；首页也可访问。修复窄屏检查器遮挡设置关闭按钮的层级问题。
- 设置改为分类导航，写作项接回原字号、字体、行距、段距、缩进、打字机和段落聚焦状态；不是新造一套偏好。没有添加尚无实现的自动纠错/符号补齐等假开关。
- 右侧历史打开记忆；默认展示既有 Dexie 记忆修订，AI 候选和事实账本分开。自动修订无需逐条审阅，AI 语义提议仍需确认。本次不是数据库级 CDC 或完整 Utopia 历史引擎；旧数据仅有迁移快照，不能重建不存在的历史。正文版本仍在批注的版本页。
- 存储增加旧自动正文快照的扫描/清单/显式确认清理，调用唯一快照 owner。仅处理超过30天的字数快照，每章最近3份、手动和操作前版本保留；不删除正文、媒体、记忆与恢复副本。预览过期拒绝、持久化失败保留原数据。没有在用户数据上执行清理；按书或媒体清理仍未实现。

本轮实际验证：四配置浏览器 exit 0（1440/900/390亮色、1440暗色）；新增偏好分类、主题持久化、记忆入口、存储空扫描、单输入与展开宽度、长设定无内滚/无额外下线等断言。截图 `/tmp/pinax-workspace-refinement/`；已查看桌面写作设置、手机写作设置、暗色记忆、长文设定编辑和推演面板。日志 `/tmp/pinax-feedback-smoke.log`。推演全六宽度304/304、设定联动20/20、试稿边界21/21均 exit 0；日志分别 `/tmp/pinax-feedback-rehearsal.log`、`/tmp/pinax-feedback-linkage.log`、`/tmp/pinax-feedback-boundary.log`。

旧测试中必须显示起点提示的断言与本次明确要求相反，替换为无起点提示、默认折叠高级项、输入无障碍名称断言；原提交/人物测试仍保留。设定联动脚本增加显式展开当前场，不用强制点击隐藏控件绕过交互。快照删除反例加入现有集成测试，不扩大20文件/200用例预算。最终 `npm run verify:full` exit 0，20文件/200用例通过、lint零警告、Vite/VitePress构建、架构与diff门禁通过，日志 `/tmp/pinax-feedback-verify.log`。

未提交推送、未操作用户5173服务或真实浏览器存储。遵循现有 UI、测试、世界书和交接规范；不需要新 skill 规则。

## 15:10 暗色、标签与恢复稿复验

- 用户补充的作家助手/ChatGPT截图作为暗色优先证据。暗色token从蓝钢改为中性石墨：主背景约`#1e1f21`，侧栏/稿面以小幅明度差分层；蓝色只保留选择、主动作与键盘焦点。未引入渐变、霓虹或装饰卡片。
- 标签带合并两套叠加规则为唯一实现：34px桌面标签、活动页细阴影轮廓并用底部同色片与内容面连通；非活动页只留短分隔，关闭按钮在活动/hover/focus时渐显。手机仍显示全部标签并横向滚动。
- 原因确认：`writeRecovery()`每次250ms防崩溃落盘后又立即设置`recoveryDraft`，导致当前会话正常输入被当成启动恢复事件。本次去掉展示态写入，落盘保护仍在；成功自动保存仍清除副本，`load()`对真正旧且不同的副本仍按guard显示。
- 浏览器脚本新增真实输入：键入后350ms断言无`save-rescue`，再等待自动保存断言状态为“已保存”。1440/900/390亮色与1440暗色全部exit 0，日志`/tmp/pinax-dark-tabs-smoke.log`；已实际查看首页、正文推演、设置和地图暗色截图。
- 定向2文件/22测试通过。最终`npm run verify:full` exit 0：20文件/200测试、lint零警告、Vite/VitePress、架构和diff门禁通过，日志`/tmp/pinax-dark-tabs-verify.log`。

未提交推送，未操作用户5173服务或真实浏览器存储。用户视觉判断优先；本次纠正落实既有视觉证据与状态语义规范，不需要新增skill规则。

## 17:01 批注、生图与设定目录复验

- 批注原位编辑使用一圈低对比边界、统一焦点轮廓和紧凑动作，不再叠加上下分隔线；数据、锚点、快捷保存和取消语义未变。
- 用户可见“画师”统一为“生图”。桌面浮层限制为约1180×780的中等工作台，参数区保持440–480px；五种风格继续复用仓库既有图集，并在右侧空态显示当前风格的大幅真实参考。参考图上传、最多3张、能力判断和生成历史没有伪实现。
- 设定目录模式单独把检查器扩至520–600px，内部详情/目录不再极端失衡，目录至少210px；章节栏继续采用全局240px基准。新建设定从孤立加号换为图标加文字的次级动作，活动项降低蓝色面积。
- 暗色继续使用中性石墨层级，蓝色限于选中、主动作和焦点；顶部标签保持34px、短分隔和内容面连通，降低活动轮廓强度。

验证：`workspace-refinement-check.mjs` 在1440/900/390亮色及1440暗色全部通过，新增目录几何、新建动作、生图窗尺寸、真实风格资产、无横向溢出和零页面异常断言；实际查看首页、设定目录和生图的明暗截图。定向3文件28测试通过。最终`npm run verify:full` exit 0：20/20文件、200/200测试、lint零警告、Vite/VitePress构建、结构预算和diff检查全部通过。测试使用隔离浏览器存储和离线provider，未触碰用户5173服务或真实书稿。
