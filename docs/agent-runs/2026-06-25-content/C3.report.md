# C3 — demo persona + font + pose 3 份 brief 落档 (Phase 2 前置 brief)

**Date**: 2026-06-25
**Session**: C3 worker (内容线程)
**Branch**: `main` (无 commit / 无 push)
**Status**: ✅ **3 份 brief 落档 + 0 src 改动 + test 仍 green (118 files / 1078 tests)**

## TL;DR

按 task spec 起草 3 份 brief 到 `docs/agent-runs/2026-06-25-content/`, 给后续 5B v0.2 手画立绘 + Phase 2 字体 finalize + GM persona 调参用。本 session 纯文档, **0 改代码, 0 下载字体, 0 生成图片, 0 硬编码 API key**。3 份 brief 都不给定稿, 给候选清单 + 理由 + 落地步骤。

## 3 份 brief 路径

| Brief | 路径 | 核心结论 |
| --- | --- | --- |
| **demo-persona-brief.md** | [`docs/agent-runs/2026-06-25-content/demo-persona-brief.md`](./demo-persona-brief.md) | **边境王国 GM persona 是隐式的**, 没有显式 persona 字段。当前行为由 hard-coded systemPrompt + worldbook.writingStyle + forbidden + 3 constant entries 联合表达。建议扩 `worldbook.persona` 字段 + `gameStore.systemPrompt` 走 persona 化 + `GmPersonaLauncher.vue` 扩 1 个 persona prop (8 字段)。3 段差异: 开场冷峻给压力 / 中段对峙给阻力 / 收尾悬念不闭合 |
| **font-shortlist-brief.md** | [`docs/agent-runs/2026-06-25-content/font-shortlist-brief.md`](./font-shortlist-brief.md) | **现栈 4 套 token + 1 LXGW @font-face 596KB**, LXGW 实际未被 token 引用只作 fallback。**3 套 shortlist**: DISPLAY (保留 ZCOOL XiaoWei, 不推荐 QingKe/Long Cang/Liu Jian Mao Cao per v3.11-13 reject 记录) / BODY (新增 `--font-body` token 指向 Noto Serif CJK SC subset ~600KB) / LXGW 替代 (subset GB2312 ~250KB + 改 `font-display: optional` lazy load) |
| **pose-brief.md** | [`docs/agent-runs/2026-06-25-content/pose-brief.md`](./pose-brief.md) | **5B v0.1 6 张立绘全少女锚 + 场景, 0 张具体人物立绘**。边境王国 6 核心人物 (伊薇/苔娜/索德/卢岑/赫玛) 全无立绘。**v0.2 候选 9 pose**: narrator-confrontation + narrator-suspense (HIGH) + sod-ya (Round 4) + tiana-witness (Round 5) (HIGH) + 3 opening-cover 场景变体 + lucin-envoy + herma-professor (MEDIUM) |

## 每份 brief 核心结论 (1 句)

1. **demo-persona-brief.md**: 边境王国 persona 文案 = 克制悬疑 + 潮湿港雾 + 证据可验证 + 不给免费兼得 + 不掉回背景设定, 3 段差异 (开场/中段/收尾) 各自有温度/句长/句式锚点, 给 GmPersonaLauncher 扩 persona prop 8 字段即可吃现有 3 preset 切换。
2. **font-shortlist-brief.md**: ZCOOL XiaoWei 留 DISPLAY, 新增 `--font-body` token 指向 Noto Serif CJK SC subset ~600KB (替代 ZCOOL 当 body 用的根因), LXGW WenKai 596KB 改 lazy load (现 596KB 在 Welcome 立即下载但 4 主页面只有 `/opening` 真用)。
3. **pose-brief.md**: v0.2 第一批手画 4 张 (narrator-confrontation + narrator-suspense + sod-ya + tiana-witness) 解决"GM 引用 6 人物但 5 个无立绘"最大缺口, 第二批 5 张 (3 opening-cover 变体 + lucin-envoy + herma-professor) 解决 3 action 按钮都是同 1 张图的体验问题。

## 0 改代码验证

```bash
$ git status --short
 M docs/demo/border-kingdom-adventure.md       (C1 worker WIP)
 M src/__tests__/playableWorldEntry.test.js     (C1 worker WIP)
 M src/pages/Experience.vue                     (V1 chrome worker WIP)
 M src/services/playableWorldEntry.js           (C1 worker WIP)
 M src/services/seedWorldbookPresets.js         (C1 worker WIP)
?? docs/agent-runs/2026-06-25-content/          (C1 + C2 + C3 reports)
?? ... (其他 worker untracked reports)
```

C3 session 0 个新 src 改动 (3 个 modified 是 C1 / V1 chrome worker 留下的 working tree WIP, 3 个 untracked dirs 是 C1 + C2 + C3 reports + 其他 worker reports)。`docs/agent-runs/2026-06-25-content/` 目录本 session 新增 3 个文件:

```
demo-persona-brief.md    12 KB
font-shortlist-brief.md  13 KB
pose-brief.md            15 KB
```

## Test 仍 green

```bash
$ npm run test:run
Test Files  118 passed (118)
     Tests  1078 passed (1078)
```

跟 C2 session 跑过的结果一致 (C2 报告 1078 tests / 0 fail, C3 session 0 改代码, 不应破坏任何测试)。

## 硬约束达成

- ✅ 0 改代码 (git diff --stat src/ 跟 C2 末态一致, 都是 worker WIP)
- ✅ 0 下载字体文件 (本 session 0 fetch 任何 woff2 / otf, brief 列候选只标 "需人工核")
- ✅ 0 生成图片 (本 session 0 调 image_generate / image_edit / mmx-cli, brief 列 pose 清单)
- ✅ 0 硬编码 API key (本 session 0 写 / 读 API key, 0 提及具体 key 字符串)
- ✅ 字体 license 标 "已确认 OFL/SIL" 或 "需人工核" (LXGW WenKai 已确认 OFL 1.1, 其它候选如 Alimama ShuHei / 501 Greatest / 幼圆 标 ⚠️ 需人工核)
- ✅ pose brief 不给定稿, 给候选清单 + 你拍板项 (5 类 9 pose, 排序 + 风格约束 + 落地建议, 无定稿图)
- ✅ 0 raw hex / 0 !important (本 session 0 CSS 改动)

## Out of scope (NOT shipped, logged for follow-up)

### demo-persona-brief 落地的 4 个 follow-up

1. `src/services/seedWorldbookPresets.js` 加 `persona` 字段 (边境王国 + 都市异闻 + 赫利俄斯 各 1 套)
2. `src/services/worldbookContextBuilder.js` 加 `【GM 角色】` block 注入 (放在 `【写作风格】` 之前)
3. `src/stores/gameStore.js:1644-1649` 改 hard-coded prompt, 接 `worldbook.persona` 派生
4. `src/stores/worldStore.js` 加 `setPersona` action (类似 setVariant), `GmPersonaLauncher.vue` 扩 1 个 persona prop 8 字段

### font-shortlist-brief 落地的 3 个 follow-up

1. Phase 2 short 优先: 跑 `pyftsubset` 把 LXGW 596KB → GB2312 subset ~250KB, `index.html` 删 preload
2. Phase 2 short 优先: 新增 `--font-body` token, `AppShell.vue` + `Experience.vue` 等使用 `--font-serif` body 处改 `var(--font-body)`
3. Phase 2 mid: LXGW 改 `font-display: optional` lazy load, 接受 hero 闪一下 ZCOOL → LXGW (~500ms)

### pose-brief 落地的 2 个 follow-up

1. C3 brief review 通过后, 用户手画 4 张 HIGH 优先 (2 周) → ship commit 1
2. 用户手画 5 张 MEDIUM (4 周) → ship commit 2
3. 跟 demo-persona-brief 联动: 扩 GmPersonaLauncher 走 persona poseHints 字段 (per persona 自动选 pose)

### font 候选 license 人工核清单 (5 项)

| 字体 | 待核 |
| --- | --- |
| Alimama ShuHei | 阿里妈妈站点 license + 商用授权 + 体积 |
| 501 Greatest | 设计师站点 license + 商用授权 + 体积 |
| 幼圆 (YouYuan) | macOS 12+ / Win11 / Android / Linux 实际 fallback 行为 |
| Noto Serif CJK SC Variable | Google Fonts 是否提供 Variable 子集 + 实际体积 |
| LXGW WenKai Screen | GitHub releases 是否仍维护 + 跟 LXGW WenKai 区别 |

未确认字体本 brief 不进入推荐 (标 ⚠️), 需先核完再 shortlist。

## Verify

- `git status --short` 显示本 session 0 个新 src 改动
- `npm run test:run` 118 files / 1078 tests / 0 fail (跟 C2 一致)
- 3 份 brief 全部落档, 每份 ≥10 KB
- 0 commit / 0 push (per task spec)
- 0 临时脚本 / 0 截图 / 0 字体下载 / 0 图片生成
