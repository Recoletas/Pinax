# G4.6.13 R7：流事件、联机状态与生产审计

## 交付

- 新增 `shared/narrativeAgentStreamContract.js`，定义版本化 SSE 事件、序列号、脱敏字段和浏览器侧归约器。
- 新增 `POST /api/generate/agent-step/stream`。服务端只将一次 provider-neutral step 转成标准事件，不执行浏览器资料工具，也不把 provider 原始流块暴露给浏览器。
- 体验主链默认使用 SSE step reader；事件在内存中还原为 `final_ready` 或 `tool_calls`，继续沿现有单 transcript 状态机执行。
- ContextLedger 与 production metrics 增加运行审计字段；联机状态保留房主唯一生成，并放行 R7 的低敏状态阶段。
- 体验状态条补齐新阶段、停止生成和失败重试入口。

## 边界

`tool.input.delta` 是内部协议事件，不进入正文、状态文案、联机广播正文或持久化内容。reasoning、完整查询、工具结果、provider signature 仍不进入 UI；成员不拥有 transcript，也不执行工具调用。

## 验证

- `npm run test:run -- src/__tests__/agentContracts.test.js src/__tests__/gameStoreSession.test.js src/__tests__/onlineRoom.test.js`：24/24。
- `npm run verify:full`：39 个核心测试文件 / 306 个用例，12 个视觉用例，Vite build、VitePress build、`git diff --check` 均通过。
- 未启动或重启现有 dev server / backend。

## 下一步

R8 只做真实 provider 矩阵与发布门禁：OpenAI Chat/Responses、Anthropic Messages、MiniMax Anthropic-compatible 的事件顺序、tool-call 重组、取消、超时、限流和 host loss smoke；不再扩大协议面。
