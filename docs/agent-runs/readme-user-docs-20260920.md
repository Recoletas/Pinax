# README 与作者文档整理回执（2026-09-20）

## 2026-09-21 作者定位修正

后续按作者反馈重写快速开始：以核心工作台、通用 AI、世界书增强创作和扩展功能为主，备份只保留必要提示。独立内测章节从 manifest 撤下，首页/设置指南直达快速开始，旧 `10-beta-guide`（含 `.md`）通过手册解析器进入快速开始；静态旧文件保留迁移链接。记忆说明改为参考 Utopia 的 Dexie/IndexedDB 数据库，区分正式事实与候选、数据库能力与完整上游实现。“卡片画布”章节改为“视频与编导助手”，说明分镜、编导顾问和视频交接。

本次浏览器检查：新快速开始及两种旧地址均显示核心功能正文，视频章节标题与导航正确；1280×800 截图已查看，正文和导航无截断。作者文档 76 个本地链接检查通过。完整验证日志：`/tmp/pinax-guide-core-20260921.log`。

本次 `verify:full` exit 0：20/20 files / 200/200 tests / build OK / diff clean / docs OK。未进行真实模型或发布站验证。

介绍页改为基础流程、世界书增强流程和扩展流程，移除给用户安排字数、测试操作的内容。核心工作台同时承担基础写作和通用 AI，关联世界书增强特色 AI；设定以资料页为起点，随后是各项设定、高级条目和未完全融合的地图。体验页按跑团说明，漫画/视频降为边缘扩展，manifest 同步显示名称与分组。

本次只改文档。当前代码的设定 tab 顺序和默认入口尚未按此调整，手册说明可点击“资料”进入；旧 `/writing` 仍是代码兼容跳转，不在作者指南作为独立入口介绍。下方 2026-09-20 验证记录仅对应首轮修订。

9 月 21 日验证：104 个作者文档本地链接、11 个 manifest 条目检查通过；`verify:full` exit 0（20/20 文件、200/200 用例、lint、Web/VitePress 构建、架构和 diff）；未重新运行浏览器或调用模型。日志位于 `/tmp/pinax-docs-20260921-verify.log`。

## 范围

- 事实基线：`main@04ed7c88e70abb3c51c81406868d98fe871f0ed3`。
- 修改 README、工程文档入口、VitePress 导航和现有作者手册；未改业务代码、路由、数据结构、远端仓库设置或部署。
- 应用内手册继续由 `DocsPage.vue` 读取 `docs/user-manual/manifest.json`；保留所有章节 ID 和文件名。

## 关键变化

| 修改前 | 修改后 |
| --- | --- |
| README 首屏混入技术徽章、工程流程和验证数字 | 先回答用途、使用方式、是否必须用 AI、保存边界，再把本地运行放到开发区 |
| 工程导航先列计划、状态和代码地图 | 第一屏分为使用 Pinax、自行部署、参与开发三个入口 |
| 手册把体验模式列为核心玩法 | 按开始使用、写作与资料、设置与帮助、扩展体验、参与测试排序 |
| 首次教程同时解释引导状态和导入边缘行为 | 主线只做新建、写一段、确认保存、导出备份；导入与异常按需展开 |
| 设置页称固定亮色、内置模型开箱即用、只讲 JSON | 按当前六个设置区说明日间/夜间、缩放、服务端密钥前提、ZIP/JSON 范围和旧版本清理 |
| FAQ 先教普通用户修改服务器并优先刷新 | 先保稿、核对原浏览器和原网址、查看恢复/备份，再刷新或联系维护者 |

## 已核对事实

- TXT / Markdown 是整本书稿导入；DOCX / PDF 是来源资料摄取，不合并描述。
- 完整工作区 ZIP 包含书稿、设定、来源归档、已落盘媒体、记忆历史和事实账本；轻量 JSON 不含来源/媒体原件和已归档记忆历史；两者都排除模型密钥。
- 当前设置包含写作、外观、记忆与历史、AI 配置、体验、存储；外观支持日间/夜间和界面缩放。
- 内置 MiniMax 在界面可见，但实际调用依赖部署者配置服务端密钥和网络。
- 浏览器本地数据按网页来源隔离；协议、主机名或端口改变前应从旧网址导出备份。

## 验证

- `manifest.json` 解析：通过。
- 本轮所有变更 Markdown 的本地链接：248 个检查，0 缺失；其中 README、工程入口和全部用户手册 94 个。
- `git diff --check`：通过。
- 应用内手册 smoke：在隔离浏览器上下文打开 `/docs/README`，五个新分组均渲染；点击“快速开始”进入 `/docs/01-quickstart`，四段核心任务正文可见。
- `npm run docs:build`：exit 0。
- `npm run verify:full`：exit 0；20/20 测试文件、200/200 用例，lint delta、Web build、构建体积、架构、diff 与 VitePress build 均通过。

## 尚未确认

- 仓库没有可核实的统一公开体验网址；文档只区分受邀地址与自行运行，不承诺服务已开放。
- 未连接实际发布站，无法确认某一部署是否已配置 MiniMax 密钥；应用内 smoke 使用已有本地服务，不调用模型。
- README 复用仓库内现有真实欢迎页和工作台截图；未运行发布站重拍，也未做陌生作者可用性观察。
- `docs:build` 只构建 `docs/src`；作者手册另以 JSON 解析、链接检查和完整 Web build 覆盖，不能把 VitePress 成功单独当作手册验收。

## GitHub About 建议文案

> Pinax：本地优先、作者主导的长篇创作工作台，把正文、人物、设定与可控 AI 辅助放在一起。

需要仓库设置权限；本轮未修改远端 About。
