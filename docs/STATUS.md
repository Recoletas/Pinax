# Status

<!--
  docs/STATUS.md — multi-session shared state.
  Read at every session start (per AGENTS.md First action step 2).
  Agent may write; user may write. Manual edits only for first version.
  Long-term history does NOT live here; migrate to docs/LOG.md / docs/PLAN.md / docs/src/known-issues.md.
-->

## 当前安排

| Owner/session | Worktree | Branch | Deadline |
|---|---|---|---|
| n/a | n/a | n/a | n/a |

> 当前安排只记录"谁在哪个 worktree 做什么、什么时候交付"。详细上下文仍写在 In flight / Recently done / Next up，避免 STATUS 顶部变成长 handoff。

## In flight
<!--
  Per-entry fields (all required unless noted):
    - Owner/session: Codex / Claude / user
    - Worktree: /abs/path (or "n/a" if on main directly)
    - Branch: feature/foo
    - Scope: src/.../file.ts
    - Intent: 正在做什么 (1 句)
    - Last touched: YYYY-MM-DD HH:MM TZ
    - Do not touch: ... (optional, only when there are specific files/areas to avoid)
-->
- Owner/session: user
  Worktree: /home/recoletas/jiuguan/text-game-framework
  Branch: wip/map-realism-render-docs-20260608
  Scope: `src/views/WelcomeView.vue`, `src/layouts/AppShell.vue`, `src/styles/main.css`, `src/pages/{Experience,Writing,Notes,ProseEssay}.vue`, `src/components/{gm-persona/*,workbench/WorkbenchPageHero.vue,QuestLog.vue}`, related UI tests
  Intent: Thread A 正在推进 `kao` / archive-folio UI 重构；按用户 2026-06-10 晚反馈，`WelcomeView` 重新成为 Phase 1B 第一优先：先做 olive/gold kao 档案册首页(3 阶段路径见 [superpowers/specs/2026-06-10-ui-redesign-design.md](./superpowers/specs/2026-06-10-ui-redesign-design.md))，再统一 `AppShell` chrome 与 `Experience` 入口结构，再决定是否把同一套开页语言继续带到 `Writing / Notes / ProseEssay`
  Last touched: 2026-06-10 22:15 CST
  Do not touch: `src/stores/gameStore.js`、`src/services/worldbookContextBuilder.js`、`src/services/generation*`、`src/components/StatusBar.vue`
- Owner/session: Claude
  Worktree: /home/recoletas/jiuguan/worktrees/agent-velocity-0615
  Branch: chore/agent-workflow-velocity (本地 1 commit, **待 push + 合 PR**)
  Scope: `package.json` + `agent-skills/{testing-verification,worldbook-workflow,commit-conventions}/SKILL.md` + `AGENTS.md` + `docs/STATUS.md` + `docs/{plan,superpowers}/README.md` + `docs/templates/spec-template.md` (new) + `docs/superpowers/specs/README.md` (new) + `docs/superpowers/specs/2026-06-15-agent-workflow-velocity-design.md` (v3 → v5 同步)
  Intent: agent workflow velocity spec v5 实施。10 文件改动落地 4 类优化：(A) `verify` 三档分档 + testing-verification skill 改"选档"；(B) `docs/plan/README.md` 加 5 条 research doc 写作约定；(E) `worldbook-workflow` skill ↔ guide 对齐 + AGENTS.md discovery paths 补 2 条 + commit-conventions step 4 去重 + First action fallback 明示；(F) commit-conventions step 6 soft 警告 + `docs/superpowers/README.md` 加 spec/plan 节奏经验段。6 阶段框架 (G9-G14) 落地：`docs/STATUS.md` 顶部加 `## 当前安排` 前置段；新建 `docs/templates/spec-template.md` 模板（7 必含段）；新建 `docs/superpowers/specs/README.md`（含 spec→plan 转换门禁 6 条）；本 spec v3 → v5（3 处 title-block 措辞统一）。
  Last touched: 2026-06-15 18:18 CST
- Owner/session: Claude
  Worktree: /home/recoletas/jiuguan/worktrees/5a-stereo
  Branch: feat/5a-stereo-migration (pushed to origin, **PR pending**)
  Scope: `src/pages/{OpeningPage,Experience}.vue` + `src/components/folio/{CharacterPortrait,CharacterArchiveStrip}.vue` + `src/composables/useCharacterArt.js` + `src/config/characterArt.js` + `src/assets/characters/stub-silhouette.svg` + `src/__tests__/{stereoMigration,uiPolish}.test.js`
  Intent: 5A 立体感骨架 + stub 已落地 3 atomic commits (`d5d4291` modules → `5d107f9` wiring → `0a98d91` CSS), PR pending, 等用户合 main。合后 5b rebase onto main (5b 当前 off feat/5a-stereo-migration, deviation logged)
  Last touched: 2026-06-15 18:30 CST
- 2026-06-09 18:05 CST — `边境王国 · 雾潮暮湾` 内容线程想补“10-15 分钟真实 AI 手测”时，shell 无法复用浏览器 `localStorage` 里的 `apiSettings`；额外尝试公开免配置上游时，`gen.pollinations.ai` 需要有效 key，`text.pollinations.ai` 本轮返回 `429 Queue full`。因此本轮先落成“真实链路排障 + 静态内容审查”，待可用模型配置明确后再补 live 手测记录。

## Recently done
- 2026-06-25 15:30 CST — V3 worker on `main`: shipped `cc12e62` archive-folio signature stamp per user 2026-06-25 反馈 "V1/V2 修掉了'土'但做了过度减法, 把档案册性格也砍了, 顶部 tab 变 Material 通用风 + 灰框圆角 = 跟任何 SaaS 一样普通. 需要给档案册找 1 个贯穿全局的签名手法". V3 单 commit 4 文件 (+737/-233) 落地 5 项签名手法贯穿 mast:
  - **印章 active** (AppShell.vue + kao.css): 删 V2 `42ed7f3` 引入的 `.shell-tab-indicator` 2px accent 滑动条 + JS `updateTabIndicator` / `indicatorStyle` ref / `tabbarRef` / resize+scroll 监听 ≈25 行; active tab 前缀 `◆` archive-rose `::before` (L455 + L913), label 字重 500→600 纯字重 bump 无底色高亮, 印章颜色用 `--accent-rose` 不用 `--accent` (SaaS 强调色 vs 档案册印泥色)
  - **罗马序号** (AppShell.vue): script 加 `const ROMAN_ACTIVITY_STAMPS = ['Ⅰ','Ⅱ','Ⅲ','Ⅳ','Ⅴ']`, 模板绑 `ROMAN_ACTIVITY_STAMPS[index] || '·'`, 删 `String(index+1).padStart(2,'0')`; `.shell-tab__index` 字体改 `var(--font-display, "Iowan Old Style", "Songti SC", "STSong", Georgia, serif)` LXGW WenKai, 12px, 删 skewX (V1 已删) + tabular-nums (罗马数字不需要等宽); mobile 760 不再 `display: none` 隐藏
  - **chip 墨点** (AppShell.vue + kao.css): `shell-meta-chip` / `shell-storage-chip` / `ws-topstrip__settings-link` / `__session-chip` / `__chip-btn` 5 处全改印章风 — `position: relative` + `::before` 内容 `·` archive-rose 14px absolute left 8-10px + `border: 1px solid color-mix(in srgb, var(--archive-rose) 22%, var(--border))` 印泥 22% 边 + `background: transparent` 透明底 + `border-radius: 0` 硬切角; hover `border-color: archive-rose 40%` + `::before { color: var(--accent-rose); font-weight: 900 }` 墨点变实心粗体
  - **mast paper-fiber** (AppShell.vue + kao.css): `.shell-mast` 删 `backdrop-filter: blur(18px)` + 2-stop SaaS gradient + box-shadow; 删 `.theme-kao .shell-mast` AppShell scoped override (含 `backdrop-filter: blur(10px)`) 让 kao.css 接管; kao.css 新增 flat `archive-paper-soft` 底 + 1px archive-gold 14% bottom edge + `.shell-mast::before` 3-stop radial-gradient speckle (复用 `.is-archive-paper::before` 同一纸纹理, mix-blend-mode multiply + opacity 0.32, 让 mast 跟下面是同一张纸)
  - **撕边虚线** (AppShell.vue): `.shell-tabbar` gap 8px→0; `.shell-tab` `border-left: 1px dashed color-mix(in srgb, var(--border) 86%, transparent)` (kao override: archive-gold 18%); `.shell-tab:first-child { border-left: none }` 无 leading stub
  - **保留** clip-path 仅在 `.shell-menu-btn` (汉堡) / `.shell-drawer` (drawer 本身) / `.shell-drawer__close` (drawer close 按钮) / kao.css `.is-bookmark` / `.material-entry-card` (V3 范围外 archive-prop); tab/chip/mast 0 切角
  - **uiPolish**: V3-A/B/C/D/E/F/G/H 8 个新契约 + V1-D 改写 match archive-rose 22% mix (原 var(--border) 改 stamp 边)
  Verify: `npm run test:run` 118 files / 1078 tests / +8 new V3 contracts / 0 regression; `npm run build` clean (3.56s); `git diff --check` clean; forbidden sweep 0 match (:global / !important / raw hex, sed 已清理 `var(--accent-rose, #c24b6a)` fallback hex). 手动验 a-e 全过 (5 tab 印章 + 罗马 + dashed 撕边 + chip 墨点 + paper-fiber 底 + 0 tab/chip/mast 切角高亮渐变边). 报告 `docs/agent-runs/2026-06-25-visual/V3.report.md`. Out of scope (logged for follow-up): ActivityBar 抽屉内部仍 V1 圆角 chip; legacy 主题不动; 5A PR merge → 5b rebase 待用户合; `ROMAN_ACTIVITY_STAMPS` 硬编码 5 项, ACTIVITY_ITEMS 扩到 6+ 需切 toRoman() 函数. pre-existing unstaged changes (Experience.vue V1 worker WIP + playableWorldEntry.js / playableWorldEntry.test.js / seedWorldbookPresets.js + docs/STATUS.md V2 worker + 4 unstaged reports under `docs/agent-runs/2026-06-24-usability/` + `2026-06-25-usability/` + 新 `2026-06-25-content/`) 按 memory `stage by name` 不动.
- 2026-06-24 13:14 CST — V2 worker on `main`: shipped `42ed7f3` page-transition polish + tab indicator + caption fade per user "页面切换没衔接" 反馈. Root cause: page-route 0.16s opacity + 4px translateY 在人眼就是瞬切, mast tab 颜色变无动效, 视觉上像没动. 1 commit 3 changes:
  - `src/styles/main.css`: page-route enter 0.28s (opacity + translateY 8px→0 + scale 0.995→1), leave 0.16s (translateY 0→-6px), cubic-bezier(0.22, 1, 0.36, 1) (matches opening titleBreathe); reduced-motion fallback 保留; new `.caption-fade` 0.18s opacity enter
  - `src/layouts/AppShell.vue`: shell-tabbar 2px 滑动指示器 — JS 算 active tab offsetLeft/offsetWidth - scrollLeft, transform + width 0.24s cubic-bezier 切; kao 主题 archive-olive variant; 移动端 overflow-x scroll + window resize + scroll 实时重测; `.shell-tabbar` 加 `position: relative` 作 offsetParent; shell-brand-route `<strong>` 加 `:key="currentRouteCaption"` + `<Transition name="caption-fade" mode="out-in">` 让 route 切文字淡入替; reduced-motion fallback 保留
  - `src/__tests__/pageRouteTransition.test.js`: 4 固定 CSS 值匹配同步更新
  Verify: `pageRouteTransition.test.js` 2/2 pass; `npm run build` clean (3.66s); per memory `stage by name` 只 stage 我的 3 文件, 其他 worker 的 V1-C/D/E 等 WIP 不动; 我编辑后 user/linter 重应用 V1-C/D/E WIP (per system reminder "intentional"), 一并进 commit; pre-existing 5 uiPolish failures (V1-A / V1-C / V1-D / V1-E×2) 来自其他 worker WIP, 与本 commit 无关. Out of scope: drawer 内 caption 不加 fade (drawer 罕见打开, fade 反而干扰); 不为 indicator 加 enter/leave 动画 (mast 常驻不该有动画).
- 2026-06-25 13:00 CST — Claude on `main`: shipped U10 routing/UX fix per user 2026-06-25 反馈 "右上角的设定不应该是设置吗，不要把设置放在设定页... 也不要做跳转，用之前的那种浮窗打开" + "上面的体验页为什么进open页，路由太奇怪了". U10 单 commit:AppShell 右上角 chip 从 `<router-link to="/settings/structured">` 改 button + 触发 SettingsPopup (3 tabs: 外观 / AI 配置 / 存储,沿用 modal-fade + overlay 模式,挂 `<SettingsPopup v-if="settingsPopup.isOpen.value">`);新建 `src/composables/useSettingsPopup.js` (module-scope `isOpen` + `activeSection` ref + open(section)/close/toggle,所有调用方共享) + `src/components/workbench/SettingsPopup.vue` (700px max,header + tabs + body,Esc 关闭 + close 按钮 focus return);U4 回退:StructuredSettings 删 AI 配置 tab + ApiSettingsPanel import + `?tab=ai` query 逻辑,只剩纯世界书结构化设定 + 新增「结构化设定」title;workbenchNav `experience.defaultRouteName` 从 'opening' 改 'experience',开场页通过 SIDE_PANELS.experience 已有的「开场页」item 走(零新路由);`InputArea` + `WelcomeView` 的 missing-key 链接从 `<router-link to="/settings/structured?tab=ai">` 改 `<button @click="openApiSettings">` 调 `useSettingsPopup().open('ai')`;storage chip ≥70% 也改开 popup 的 'storage' tab(去掉 AppShell 里 inline 的 storage modal markup + CSS,改由 SettingsPopup 提供);新测试 `useSettingsPopup.test.js` (7) + `settingsPopup.test.js` (5) = +12 tests,既有 `workbenchNav.test.js` + `welcomeView.test.js` 2 个失败断言同步更新。Verify:`npm run test:run` 118 files / 1065 tests / +0 regression;`npm run build` clean (3.64s);`git diff --check` clean;forbidden sweep 0 match;pre-existing unstaged changes (playableWorldEntry.js / playableWorldEntry.test.js / seedWorldbookPresets.js + 4 unstaged reports under `docs/agent-runs/2026-06-24-usability/` + `2026-06-25-usability/`) 未 stage,按 memory 提示 stage by name。Out of scope (logged for follow-up):VitePress docs 里 3 处 routing/path 文案 (`docs/user-manual/03-features.md` + `docs/guides/worldbook-workflow.md` + `docs/superpowers/specs/2026-06-11-welcome-experience-pass4-resume-and-density-design.md` 提到「右上角设定 chip → /settings/structured」),后续文档 sync pass 再补。
- 2026-06-24 10:00 CST — Claude on `main`: shipped 3 atomic usability commits (U4 API key config + U5 onboarding + U6 flow clarity) on top of the earlier U1-U3 route/exit round. U4 (`56804a1`) extracts API config from old Settings.vue into ApiSettingsPanel reachable from StructuredSettings with ?tab=ai deep-link and an InputArea missing-key hint. U5 (`4d922d9`) adds a 3-step first-run onboarding guide on WelcomeView that auto-hides when all steps complete. U6 (`48fd2c7`) clarifies WorldBookQuickImport exit labels and adds an Experience topstrip session chip with a persistent 切换 button so the SessionPicker is reachable even when the user has an active session. All 3 commits verified: `npm run verify:full` 113 files / 1027 tests / build OK / diff clean / docs OK / visual OK. Reports under `docs/agent-runs/2026-06-24-usability/UI-U{4,5,6}.report.md`. Unstaged reports remain untracked.
- 2026-06-25 11:00 CST — Claude on `main`: shipped 3 atomic commits (U7 + U8 + U9) closing the writing-efficiency + mobile + data-safety round on top of the prior U4/U5/U6 ship. U7 (`fd43d9f`) ships a real undo/redo stack for the Writing textarea via `useEditorHistory` (60-snapshot cap, 500ms debounce, redo-stack discard on new input, chapter-switch clears history, Ctrl/Cmd+Z + Ctrl/Cmd+Shift+Z + Ctrl+Y) — replaces `document.execCommand('undo'/'redo')` which is contenteditable-only and silently broken on textarea; the other 5 `execCommand` calls (bold/italic/underline/removeFormat/cut/copy/paste/delete) are marked `TODO(undo-redo)` for follow-up. 13 new tests for `useEditorHistory`. U8 (`b98a62f`) adds HTML5 drag-and-drop reorder to the chapter list (`onChapterDragStart/Over/Drop` + `reorderChapter` persisted via existing `saveChapters`/`saveBooks`) with per-item ▲/▼ move buttons for touch; AppShell mast now collapses progressively at ≤760px (compact tabs, horizontal scroll, index hidden) and ≤480px (tabbar hidden entirely, hamburger + caption + settings chip remain). U9 (`0ac15e4`) ships storage health monitoring + full backup export: new `useStorageHealth` composable computes UTF-16 byte usage, warns at ≥70%/≥90%, wraps `safeSetItem` to catch QuotaExceededError; new `backupExport` packs all 48 Pinax-owned localStorage keys (39 STORAGE_KEYS + 9 legacy/undeclared) into `pinax-backup-YYYYMMDD-HHmm.json` via extracted `utils/download.js`; AppShell mast shows a storage chip at ≥70% that opens a detail modal with top-10 key sizes and an "导出全部备份" button. 13 new tests for storage health + backup export. All 3 commits verified: `npm run test:run` 116 files / 1053 tests +0 regression; `npm run build` clean (3.65s); `git diff --check` clean; forbidden sweep `git diff HEAD -- src/ | grep -E ':global(.theme-kao)|!important|#[0-9a-fA-F]{3,8}\\b'` returns 0 matches (all new CSS uses archive-folio tokens only, hex fallbacks removed). Reports under `docs/agent-runs/2026-06-25-usability/UI-U{7,8,9}.report.md`; U4/U5/U6 reports under `docs/agent-runs/2026-06-24-usability/`. Out of scope (logged for follow-up): rich-text execCommand replacements (U7 TODO), backup import (U9), IndexedDB migration for >5 MB quota (Phase A of local-first plan).

<!--
- 2026-06-17 21:10 CST — Claude on `feat/n5c-material-archive-folio` (N5C material page ship, **NOT pushed — 8 commits awaiting user review**): Notes.vue (2748 → ~720 行 scoped CSS + 3 imports + 3 computed/reactive entries) 从 SaaS 工具壳切到 kao archive-folio "资料册/相片夹/索引页" 视觉。3 phase A/B/C 实施:Phase A(4 commits)在 `src/styles/themes/kao.css` 加 4 utility (`.material-action-btn` 18px hard-offset CTA 签名 + `.material-bookmark-toolbar` 4-button grid + `.material-entry-card` 撕边缩略 + `.material-tear-svg` SVG defs wrapper,全部 `.theme-kao` gated) + `src/__tests__/uiPolish.test.js` 加 5 条 N5C 契约 (FolioSurface / ArchiveStrip / CharacterPortrait / .material-action-btn / scoped CSS ≤ 770) + Notes.vue 4 batch action button 加 `.material-action-btn` class + scoped CSS 1338 → 721 行瘦身(删 dead `theme-toggle` / `toolbar-text-btn` / `add-btn.btn-new` / `tool-btn` / `mode-switch` / `asset-canvas-primary/secondary` 等 7 unused class block);Phase B(5 commits)template 重排 5 个改动点:editor-main 包 `<FolioSurface variant="paper" decorated>` (B1) + sidebar header 与 `.book-list` 之间插 `<CharacterPortrait pose-id="narrator" size="thumb" caption="档案员">` (B2, 跟 `useCharacterArt().resolveArt({poseId:'narrator'})` 自动 wire 到 `kao-archive-narrator.webp` 144 KB ship 立绘) + editor-main `.title-row` 下方插 `<ArchiveStrip :items="archiveStripItems" :image="firstImageDataUrl">` 显示当前 kind 最近 3 entry collage tile (B3, `archiveStripItems`/`firstImageDataUrl` computed + `currentKindForArchiveStrip` ref 跟进 kind 切换) + 新建素材 modal 包 `<FolioSurface variant="paper" decorated as="div">` (B4) + WorkbenchPageHero "新素材" 按钮 class `toolbar-text-btn prominent` 换 `.material-action-btn` (B5);Phase C 验收:Playwright 跑 1280×800 截图到 `docs/demo/n5c-material-page-20260617_001.png` (263 KB,验证 narrator 立绘 + 8 .material-group + FolioSurface 装裱 + 4 batch 按钮 + GmPersonaLauncher + WorkbenchPageHero 视觉成立) + 全 test suite 109 文件 / 757 tests pass + build clean(3.40s)。**uiPolish 4 硬契约 0 破坏**:`material-selection-bar` 4 按钮字面量 (`@click` + 文案 + `</button>`) 保留 + `<Transition modal-fade>` 保留 + `<GmPersonaLauncher>` 保留 (无 `.advisor-fab`) + `<WorkbenchPageHero kicker="Material Library">` 保留。**5 条 N5C 契约全绿**(N5C: FolioSurface/ArchiveStrip/CharacterPortrait/.material-action-btn/scoped CSS ≤ 770,文件 `src/__tests__/uiPolish.test.js:621-` 在新 `describe('N5C: material page kao archive-folio refactor', ...)` block)。**theme-system `.theme-kao` gate + `.theme-legacy` fallback 兼容**:kao.css 新 utility 自动跟随 `.theme-kao` gate,legacy variant 走精简后 scoped CSS。**BookmarkButton component 不重用**(per R3-A):4 batch action 改用 raw `<button>` + `.material-action-btn` class 避免 uiPolish L77-80 字面量冲突。**跨页面 0 冲突**:Vue scoped `[data-v-xxx]` 自动隔离,Writing.vue / ProseEssay.vue 同 scoped class 删除不互相影响;4 新 class 名 src 内 0 冲突;`.theme-kao` gate 不泄漏。**8 worktree commits (single-branch)**: `53025e0 feat(kao.css) 4 utility` → `e484148 fix shadow 4px→18px CTA` → `3053fe1 test(uiPolish) 5 N5C contracts` → `50e5771 refactor N5C describe block` → `1c1276d feat(notes) .material-action-btn 4 buttons` → `cc5c0d8 refactor scoped CSS 1338→721 行` → `21a5b78 feat FolioSurface editor-main` → `f61695d feat CharacterPortrait narrator` → `7c722dc feat ArchiveStrip 3 entry collage` → `c75c70b feat modal FolioSurface` → `b73855a feat 新素材按钮 class`. **Spec** `docs/superpowers/specs/2026-06-17-material-page-design.md` (200 行, 5 round self-review fix 17 issues). **Plan** `docs/superpowers/plans/2026-06-17-material-page.md` (762 行, 15 task A0-A4 / B0-B5 / C1-C4). **Out of scope** (per spec NG1-NG7): Writing.vue / ProseEssay.vue 视觉同构后续 / archive-keeper 立绘 5B v0.2 user 手画 / TornEdgeFilter 共享组件 / 5C wallpaper keyframes / 入场动画 / theme-system 进一步改造。
-->
- 2026-06-24 08:55 CST — Codex on `main`: accepted and shipped the usability interface round after U1/U2/U3 worker handoff, plus the already-present E13 product-state slice in the dirty tree. U1 moves setting/worldbook URLs from `/experience/*` to `/settings/*` and keeps backwards redirects; U2 renders the persistent workbench mast with 5 activity tabs and a structured-settings chip while immersive welcome/opening routes stay mastless; U3 adds direct cross-page exits (Welcome → activities, Writing/Materials → adventure, ProseEssay → Materials, Experience → settings). E13 adds a local demo/product-state surface for empty `/experience`: `useLocalDemo`, demo banner/actions, scene-prompt entries, and right-rail demo placeholders. Reports are under `docs/agent-runs/2026-06-24-usability/`; E13 screenshots are under `docs/agent-runs/2026-06-23-ui-e13/`. Codex fixed the stale BIG1-5 contract by using the existing `isDemoMode` computed in StatusBar/GeographyPanel/QuestLog. Verification: `npm run test:run` passed 113 files / 1021 tests; `npm run build` passed (existing kao.css static+dynamic warning only); `git diff --check` clean; runtime forbidden sweep over `src/` excluding tests clean. Dev server checked at `http://127.0.0.1:5176/`; external Playwright run was not authorized, so browser-click validation was covered by focused contracts plus source/route inspection rather than a new screenshot script.
- 2026-06-23 21:35 CST — Codex on `main`: accepted and shipped E12 `/experience` visual refinement after user directed to stop over-polishing and move faster. Scope: `src/pages/Experience.vue`, `src/components/GamePanel.vue`, `src/composables/useWorkstationMeta.js`, `src/styles/themes/kao.css`, `src/__tests__/uiPolish.test.js`, E12 reports/screenshots under `docs/agent-runs/2026-06-23-ui-e12/`, and removal of the stale E11 `experience-e11-*.png` screenshots. E12 improves readability and hierarchy without changing the E11 workstation architecture: central record text moves to clearer body typography, small meta stops using LXGW brush, workstation buttons gain physical depth, the center stage gets a ruled ledger surface, right rail gets one binder/tab treatment, menu handoff gets a page title + kao hamburger treatment, and FIX2 reduces the 640px blocker to a simple vertical mobile flow while removing the misleading `1 / 1` hero folio count. QA3 accepted after focused blocker fixes. Verification before commit: `npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/gamePanelMechanism.test.js` passed 245/245; `npm run test:run` passed 113 files / 1014 tests; `npm run build` passed; `git diff --check` clean. Known visual caveat: 640px Experience is now usable and non-overlapping, but still plain; next work should be a bigger product/content slice, not more E12 micro-fixes.
- 2026-06-23 16:45 CST — Codex on `main`: accepted and shipped the E11 `/experience` workstation redesign as a structural checkpoint, not as final visual approval. Scope: `src/pages/Experience.vue`, `src/components/{GamePanel,QuestLog,StatusBar}.vue`, `src/components/geography/GeographyPanel.vue`, `src/composables/useWorkstationMeta.js`, `src/styles/themes/kao.css`, `src/__tests__/{uiPolish,experienceFullBleed}.test.js`, E11 spec/plan docs, and E11 reports/screenshots under `docs/agent-runs/2026-06-22-ui-e11/`. E11 replaces the failed axis/indicator direction with a 4-zone workstation: left narrator hero/control rail, topstrip case/progress metadata, center record flow, and right dossier sections with empty hints. W3F added explicit DISPLAY/BODY/META/INTERACTIVE font layering; W3C verified stale contracts were already converted to `ws-*`. Verification before commit: `npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/gamePanelMechanism.test.js` passed 217/217; `npm run test:run` passed 113 files / 986 tests; `npm run build` passed; `git diff --check` clean; added-line forbidden sweep found no runtime CSS violations (raw user grep also matches test assertion strings and deleted old hex lines). Visual caveat: user still finds Experience weak; next round should improve font legibility, composition impact, and menu/page transition rather than treating E11 as visually done.
- 2026-06-22 23:10 CST — Codex on `main`: integrating the accepted Lusion-r10 UI handoff after user confirmed the next direction should keep pushing on Experience. W10 is already on `main` via `c2754f7` / `03f4c94`: Writing keeps the W9 thin cork header and adds the desk-lamp memory point (`wall__lamp` SVG + `wall__lamp-cone`) so the editing wall has a first-read visual anchor. This commit ships the remaining accepted N10 + N10-FIX + E10-CLEAN work: Notes replaces the small N9 right-rail pinboard with a full `multi-canvas` material board (main active card + multi-slip right column + Lusion-style cross prompt), raises the pin cap to default-all, and N10-FIX restores real desktop drag positioning through `useCanvasBoard` while mobile falls back to static wrapping; Experience keeps only the useful E10 pieces (`--font-body`, `scene-entry` single-column message flow, and mechanism-trigger click preservation) and removes the failed `.game-page::before` axis plus `scene-stage__indicator` live template/CSS/computeds. Reports and screenshots are under `docs/agent-runs/2026-06-21-ui-lusion-r10/`; E10 screenshots are retained as reject evidence only. Verification before commit: `npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/useCanvasBoard.test.js src/__tests__/gamePanelMechanism.test.js` passed 213/213; `npm run build` passed; `git diff --check` clean; staged forbidden scan required before commit. Known next step: `/experience` still feels wrong visually and should continue with E11 workstation redesign, not more axis/indicator polish.
- 2026-06-21 20:55 CST — Codex on `main`: integrated the accepted W9 / N9 / E9-FIX archive-folio polish plus the Lusion research/spec handoff. Scope: `src/pages/{Writing,Notes}.vue`, `src/components/GamePanel.vue`, `src/composables/useCanvasBoard.js`, `src/styles/themes/kao.css`, `src/__tests__/{uiPolish,useCanvasBoard}.test.js`, W9/N9/E9 reports and 8 screenshots under `docs/agent-runs/2026-06-21-ui-fix/`, plus Lusion research reports under `docs/agent-runs/2026-06-21-lusion-research/` and [docs/superpowers/specs/2026-06-21-lusion-inspired-pinax-design.md](./superpowers/specs/2026-06-21-lusion-inspired-pinax-design.md). W9 demotes Writing's 188px cork header into a low functional strip and removes the redundant project/title/stat stack; N9 turns Notes' right void into a real 1-3 item canvas-pinboard with `useCanvasBoard` foregrounding support; E9 turns Experience messages into ledger-spread book pages and E9-FIX restores mechanism-trigger clicks on both left and right sheets. Lusion R1/R2/R3 distilled `lusion.co` into Pinax-compatible lessons while explicitly excluding WebGL/canvas/heavy 3D; DOCFIX clarified that UI-R8 `STOP NOW` remains a valid default and E10 is only an optional ACCEPT/DEFER/REJECT fork, not implemented. Verification in this session: `npm run test:run -- src/__tests__/gamePanelMechanism.test.js src/__tests__/uiPolish.test.js src/__tests__/useCanvasBoard.test.js` passed 190/190; `npm run build` passed (existing Vite kao.css static+dynamic warning only); `git diff --check` clean. Next decision: stop UI polish per R8, or explicitly approve/defer/reject the optional E10 ledger-spread transition spec.
- 2026-06-21 18:05 CST — Codex on `main`: integrated the current E6A + N6 + N6F2 worktree as one finished handoff. Scope: `src/components/GamePanel.vue`, `src/pages/Notes.vue`, `src/styles/themes/kao.css`, `src/composables/useCanvasBoard.js`, `src/__tests__/{uiPolish,useCanvasBoard}.test.js`, and focused reports/screenshots under `docs/agent-runs/2026-06-21-ui-fix/`. Experience E6A keeps the record-book direction but improves readability: message body is 16px / 1.75 line-height, small meta uses sans instead of LXGW, role kickers become block signatures, chat gets a spine stitch, message cards get role-color left bars, and chapter rules divide message groups. Notes N6 adds 1-3 draggable pinned material slips around the active card, with localStorage persistence and kao/dark styling. N6F2 cherry-pick fixed the real drag persistence bug by making `useCanvasBoard` support both `ref({})` and `reactive({})` position maps; Notes keeps its existing `reactive({})` call path. Verification in this session: `npm run test:run -- src/__tests__/useCanvasBoard.test.js` passed 16/16; `npm run test:run -- src/__tests__/uiPolish.test.js` passed 138/138; `npm run test:run` passed 113 files / 903 tests; `npm run build` passed; `git diff --check` clean; env-specific `.mjs` screenshot script scan empty. Explicitly not shipped here: W5R writing screenshots/reports remain unstaged because source changes are absent; E6B/right-rail hierarchy is deferred/canceled for now. Next UI direction should stop broad visual polish and move to real content/demo flow, 5B v0.2 art, or a product-state slice with user-approved content density.
- 2026-06-20 14:25 CST — Codex integrating Claude UI-W2/N2/E2 on `main`: QA2 accepted the strong workbench redesign for Writing / Notes / Experience as merge-ready after the required fixes landed. Scope: `src/pages/{Writing,Notes,Experience}.vue`, `src/components/{InputArea,QuestLog,StatusBar}.vue`, `src/components/geography/GeographyPanel.vue`, `src/styles/themes/kao.css`, UI contract tests, and 13 review screenshots under `docs/agent-runs/2026-06-19-ui-redesign-research/`. Direction: Writing = Pinax Wall / editing-room wall, Notes = archive drawer / card catalog, Experience = site record book / case log. Confirmed fixes: no new scoped `:global(.theme-kao)`, Notes keeper portrait no longer uses broad `:deep(*)` / `!important`, StatusBar test contract updated, Writing folder tab/title collision mitigated, missing Notes dark + Experience 1440 screenshots delivered, env-specific screenshot script removed. Verification reported by QA2 / Codex handoff: `npm run test:run -- src/__tests__/uiPolish.test.js` passed 97/97, `npm run build` passed, `git diff --check` passed, `npm run verify:full` passed (846 tests + docs/visual checks). Known follow-up polish, not blocking merge: Notes dark drawer/deck visual jump, `.wall__book-select` needs dedicated kao token rule, and all three pages still need a next aesthetic polish pass beyond this anti-micro-tweak redesign.
- 2026-06-20 16:20 CST — Claude W3/N3/E3 + Codex integration on `main`: second polish pass after the accepted strong workbench redesign, reviewed by UI-QA3 as `ACCEPT WITH FIXES` and cleaned by Codex before merge. Scope: `src/pages/{Writing,Notes,Experience}.vue`, `src/components/{GamePanel,InputArea,QuestLog,StatusBar}.vue`, `src/styles/themes/kao.css`, `src/__tests__/uiPolish.test.js`, and 9 polish screenshots under `docs/agent-runs/2026-06-20-ui-polish/`. Visible moves: Writing adds dossier tape strips + shelf scroll/note anchor + hamburger clearance; Notes unifies drawer/deck/pin onto one paper wall and gives active-card/archive-pin stronger object treatment; Experience turns side rail into one dossier with stamp labels, message stream into ledger entries, and StatusBar/QuestLog modals into dossier pages. QA3 cleanups applied: replaced new active-card `#fff` with `var(--archive-paper)` mix, added uiPolish gate against hex in `.theme-kao .active-card`, and deleted env-specific `take-n3-screenshots.mjs` / `inspect-dark.mjs` plus non-gate baseline screenshots. Focused verification: `npm run test:run -- src/__tests__/uiPolish.test.js` passed 114/114; `npm run build` passed. Known follow-up: Notes dark empty state's far right can still read as a dark void; this is non-blocking and should be handled in a later visual round if the user still objects after trying the page.
- 2026-06-20 20:43 CST — Claude W4/N4/E4 + Codex cleanup on `main`: P2 polish pass accepted by UI-QA4 with no blockers/highs and integrated after cleanup. Scope: `src/pages/{Writing,Notes,Experience}.vue`, `src/components/GamePanel.vue`, `src/styles/themes/kao.css`, `src/__tests__/uiPolish.test.js`, and 9 P2 screenshots (`writing-wall-p2-*`, `experience-record-book-p2-*`, `notes-drawer-p2-*`). Direction stayed subtractive: Writing removes redundant dossier pins/watermark and pushes portrait back so the manuscript wins; Notes turns the empty archive into kind-labeled drawer blueprint cells plus stamp/footer; Experience changes messages from numbered ledger rows into record-book pages with inline role kickers and calmer rhythm. Codex cleanup before merge: replaced new `.empty-archive__card` `#fff` with `var(--archive-paper)`, deleted env-specific `take-p2-screenshots.mjs` / `inspect-dark-empty.mjs`, and dropped unreviewed `experience-rail-dedupe-*` intermediate screenshots. Known follow-up remains: Notes dark empty state can still leave the far-right area feeling void-like; QA4 treated this as inherited/non-blocking and recommends a later `.theme-kao.theme-dark .empty-archive__cell--empty` tint pass if needed.
- 2026-06-19 21:12 CST — Codex on `main`: fixed user-reported UI regressions after the Notes/Writing/Experience worker merge. Root cause for `/experience` blank/incorrect load: `Experience.vue` used scoped `:global(.theme-kao) .sidebar-section`, which Vue compiled into a bare `.theme-kao { padding: 14px; ... }` rule and applied to `<html class="theme-kao">`, shrinking the app to a 0-width layout. Replaced that with a normal page-owned global style block scoped by `.theme-kao .game-page ...` and added a contract banning `:global(.theme-kao)` in Experience. Notes top chrome now drops `WorkbenchPageHero`, `Material Library`, and the long explanatory copy, replacing it with a compact `material-top` strip that preserves back, status, count, writing, new material, and theme actions; shared manuscript-top left padding now avoids the fixed hamburger button. Visual caveat: Writing still needs a separate aesthetic slice; this round only prevents menu overlap and removes the clearest Notes hero redundancy. Verification: browser Playwright smoke showed `/experience` html/body/app width 1280 and visible game shell; `npm run test:run -- src/__tests__/uiPolish.test.js` passed (80 tests); `npm run verify:full` passed.
- 2026-06-19 22:10 CST — Codex orchestrated a second, intentionally token-heavy Claude research pass after user rejected the previous Writing/Notes/Experience changes as "micro-tuning". Four Claude CLI workers produced page-specific reports under `docs/agent-runs/2026-06-19-ui-redesign-research/`: `UI-R0` cross-page diagnosis, `UI-W1` Writing, `UI-N1` Notes, `UI-E1` Experience, plus 1280px baseline screenshots. Codex synthesized them into [docs/superpowers/specs/2026-06-19-workbench-strong-visual-redesign-design.md](./superpowers/specs/2026-06-19-workbench-strong-visual-redesign-design.md) and [docs/superpowers/plans/2026-06-19-workbench-strong-visual-redesign.md](./superpowers/plans/2026-06-19-workbench-strong-visual-redesign.md). New direction: Writing = Pinax Wall / editing room wall; Notes = archive drawer / card catalog; Experience = site record book / case log. Hard gate: no micro-tweak-only output, no scoped `:global(.theme-kao)`, screenshot acceptance required, Codex remains integration verifier. No app code changed in this pass.
- 2026-06-19 20:58 CST — Codex on `main`: accepted Claude `Experience` V2 slice A after resolving the mixed-worker stash conflict and refactoring the risky cascade path. Scope: `src/pages/Experience.vue`, `src/__tests__/uiPolish.test.js`, screenshots `docs/demo/exp-v2-archive-binder-20260619_001.png` and `docs/demo/exp-v2-archive-binder-sidebar-20260619_001.png`; `src/styles/themes/kao.css` conflict was resolved without keeping the worker's unlayered `!important` sidebar override block. The right sidebar now reads more like an archive binder in kao mode (paper panel, gold spine/ring line, "卷宗" index badge, paper section cards) while retaining `StatusBar` / `GeographyPanel` / `QuestLog`, collapse toggle, quick notes, image rail, and GM persona entry. Codex visual caveat: direction is acceptable for this slice but internals still show dashboard remnants (`GeographyPanel` stats grid and red add-location CTA); those require a later component-internal slice. Focused verification: `npm run test:run -- src/__tests__/uiPolish.test.js` passed (80 tests).
- 2026-06-19 20:44 CST — Codex on `main`: isolated and accepted Claude `Writing` top slice from the mixed UI worker worktree after Notes A landed. Scope kept to `src/pages/Writing.vue`, W4 `manuscript-top` rules in `src/styles/themes/kao.css`, W4 contracts in `src/__tests__/uiPolish.test.js`, and screenshot `docs/demo/writing-v2-top-20260619_001.png`; concurrent `Experience` sidebar candidate remains stashed/unmerged for separate review. The accepted slice removes the SaaS-like `WorkbenchPageHero` / `Writing Desk` / long `writingHeroDescription` copy from Writing and replaces it with a compact manuscript-top strip that preserves back, book select, chapter label, status+word count, inbox, material library, storyboard export, and theme toggle. Visual caveat: screenshot review says this is a structural cleanup and not the final manuscript aesthetic; it still reads somewhat like a slim toolbar and needs later refinement of chapter/sidebar/editor object language. Focused verification: `npm run test:run -- src/__tests__/uiPolish.test.js` passed (73 tests); `npm run build` passed.
- 2026-06-19 20:36 CST — Codex on `main`: isolated and accepted Claude `Notes` slice A from the mixed UI worker worktree. Scope kept to `src/pages/Notes.vue`, N5C-A additions in `src/styles/themes/kao.css`, and N5C-A contracts in `src/__tests__/uiPolish.test.js`; concurrent `Writing` / `Experience` worker changes were temporarily stashed and deliberately excluded from this commit. Notes material groups now read more like an archive index: kind-color tab spine, roman group number, SVG chevron, and checked-selection stamp (`已选 N 项 · 批量`) with kao-only gold/paper overrides. Verification on Notes-only worktree: `npm run test:run -- src/__tests__/uiPolish.test.js` passed (72 tests); `npm run build` passed (Vite warning about static+dynamic `kao.css` remains existing). Screenshot gap: Claude did not provide a fresh `/notes` screenshot for this slice; only old baseline screenshot exists, so visual acceptance is code/test-based pending next browser pass.
- 2026-06-19 19:08 CST — Codex on `main`: fixed the incomplete `经典` theme switch. Root cause: `src/styles/themes/legacy.css` already held the map-era blue-white tokens (`#f3f3f3`, `#fff`, `#0078d4`, `#106ebe`, `#1a1a1a`, `#cbd5e1`), but `ThemeAssets.vue` still treated legacy as `null`, so the chunk never loaded at runtime. `ThemeAssets.vue` now dynamically imports `legacy.css`; `AppearanceControls.vue` labels the fallback as `经典 · 亮色/暗色` instead of `Legacy · ...`; theme-system spec notes were updated so future agents do not preserve the stale "empty legacy chunk" assumption. Added contracts in `legacySnapshot.test.js` and `appearanceControls.test.js`. Focused verification: `npm run test:run -- src/__tests__/legacySnapshot.test.js src/__tests__/appearanceControls.test.js src/__tests__/welcomeVariantToggle.test.js src/__tests__/themeStore.test.js src/__tests__/themeVariantView.test.js` passed (5 files / 41 tests).
- 2026-06-18 19:02 CST — Codex on `main` (worktree n/a): legacy.css palette alignment to map-era blue-white, 2 commits. Per user "经典感不应该是蓝白色的那个版本吗" + "找最后一次更改地图的那个提交" — the user remembered the canonical "blue-white 经典" look from the last map commit `b4a8cfb map0.1` (2026-06-07), which used `--bg-primary: #f3f3f3` + `--accent: #0078d4` (Microsoft Azure blue) + `--text-primary: #1a1a1a` + `--bg-secondary: #fff` + `--border: #cbd5e1`. Commit 1 `8615126 feat(theme): legacy variant now resolves to clean blue-white SaaS baseline` first attempt with generic `#2563eb` Tailwind blue; commit 2 `07b9f7a fix(theme): align legacy palette to map-era blue-white (#0078d4 not #2563eb)` corrected to the exact map-era values. The user's "经典感" was specifically the Microsoft Azure blue + light-gray palette from the map era, not the generic Tailwind blue. Legacy.css now overrides `:root.theme-legacy` with the 6 key tokens (bg-primary, bg-secondary, accent, accent-hover, text-primary, border) at 181 chars trimmed, within `legacySnapshot.test.js` < 200 char invariant. Verify: 112 files / 814 tests / 0 fail; legacySnapshot 15 tests pass (size invariant preserved); build 3.65s clean.
- 2026-06-18 18:37 CST — Codex on `main` (worktree n/a — direct on main per user): theme variant toggle discoverability fix, single commit `ca75eae`. Per user "经典和现在风格的切换没有生效" — the variant switch (kao/legacy) was previously only reachable inside the AppShell drawer (AppearanceControls 4-radio), so users landing on `/welcome` saw only the light/dark pill and thought the variant switch was unimplemented even though `themeStore.setVariant` + `setAppearance` were fully wired. 5 files / 280+/5-: `src/composables/useTheme.js` extends with `isKao` (computed) + `setVariant(v)` (existing `isDark`/`toggleTheme`/`initTheme` API preserved); `src/views/WelcomeView.vue` adds 2-button variant toggle group (`data-test="variant-kao"` + `data-test="variant-legacy"`, `.is-active` state) inside `.welcome-chrome` after the existing `.theme-toggle` pill, style matches the 999px gold-border aesthetic using `--archive-*` tokens; `src/views/legacy/WelcomeView.vue` adds the same toggle inside the existing `.welcome-chrome-right` div, style uses legacy tokens (`var(--bg-secondary)` / `var(--border)` / `var(--accent)`) — allowed by the §7 header exception ("accessibility / test IDs for the theme switcher"); `src/__tests__/welcomeVariantToggle.test.js` new 5-test file mirroring `appearanceControls.test.js` pattern (Pinia setup, `localStorage.clear`, `flushPromises + nextTick` settling, vue-router mock for legacy view); `src/__tests__/legacySnapshot.test.js` updates `LEGACY_VUE_HASHES['src/views/legacy/WelcomeView.vue']` from `de1e6244...36169` to `ed6287f0...8f92f`. Discovery via 2 parallel research subagents (theme system code audit + AGENTS/skill rules audit) → 2 parallel implementation subagents (code in `useTheme` + WelcomeView + legacy/WelcomeView; new test in `welcomeVariantToggle.test.js`) → Codex integration (sha256 update + verify:full + commit). **Verify**: `npm run test:run` 112 files / 814 tests / 0 fail (was 809 on main; +5 = new variant toggle tests); `npm run build` 5.49s clean; `git diff --check` clean. **Out of scope (Phase 2 deferred)**: 8 other views with identical `.theme-toggle` pills (OpeningPage, Experience, Writing, Notes, ProseEssay, StructuredSettings, plus `pages/legacy/OpeningPage.vue` and `pages/legacy/Experience.vue`) intentionally not touched this round to bound the `legacySnapshot` blast radius; moving `AppearanceControls` out of the drawer (the 4-radio escape hatch remains in AppShell for power users); `legacy.css` intentionally empty per `legacySnapshot.test.js` size invariant.
- 2026-06-19 CST — Codex on `main`: 按新 Codex/Claude workflow 清理当前工作树并吸收有用内容。吸收 `feat/n5c-material-archive-folio` 的最终 Notes 素材页 archive-folio 状态（`src/pages/Notes.vue` + N5C `uiPolish` 契约 + `docs/demo/n5c-material-page-merged-20260618_001.png`），不带入中间重复截图；从旧 `feat/5c-experience-push` 只摘低风险 Experience 会话修复（worldbook-filtered latest session restore、`SessionPicker busy`、`isStarting` + `try/finally` 防重复点击），不吸收 `61d569a` radical opening encounter 的未完成 broad UI rewrite。清理 stale 本地结构：删除 worktree `/home/recoletas/jiuguan/worktrees/5c-experience`，删除本地分支 `feat/5c-experience-push`、`feat/n5c-material-archive-folio`、`main-tmp`；`server-version` 保留。Verification: `npm run test:run -- src/__tests__/uiPolish.test.js` passed (67 tests); `npm run verify:full` passed (Vitest + Vite build + `git diff --check` + VitePress docs build + visual-verification).
- 2026-06-19 CST — Codex on `main`: 将 Codex / Claude 双工具协作和视觉对齐流程固化为项目文档与 agent 硬约束。新增 `docs/engineering/agent-orchestration-workflow.md`（Codex 主控、Claude 异步 worker、看板、summary 限长、上下文保护、worker brief 模板）和 `docs/engineering/visual-alignment-workflow.md`（direct 标注语义、视觉硬约束、小切片 prototype、截图评分、何时派 Claude/何时 Codex 精修）。更新 `AGENTS.md` 的 External Claude CLI worker pattern：多 worker 必须维护 `docs/agent-runs/current.md` 看板，Claude 输出默认只能以 summary/screenshot/diff/test 进入 Codex 验收，不允许 Claude 反向调用 Codex，视觉 UI 必须先转硬约束并按截图验收。更新 `docs/README.md` / `docs/engineering/development-standards.md` / `docs/LOG.md` 文档入口。Verification: `npm run verify:full` passed (Vitest + Vite build + `git diff --check` + VitePress docs build + visual-verification); agent-maintenance symlink/frontmatter checks passed.
- 2026-06-18 16:50 CST — Codex orchestrator + external Claude CLI workers on `main`: Nova-inspired runtime foundation landed in 3 feature commits after spec/plan baseline `2717848`. Commit 1 `e4bd36f feat(runtime): add bounded context ledger` adds `src/services/contextLedger.js`, context ledger tests, `worldbookContextBuilder` ledger parts for empty/included/truncated worldbook context, and `gameStore.lastContextLedger` without changing `messagesToSend` order/content. Commit 2 `cc8ffd6 feat(runtime): persist capped runtime event envelopes` cherry-picks Claude worker `3c2c87f` and adds `src/services/runtimeEvents.js`, event validation/capping, `runtimeEvents` session sidecar persistence, and user/assistant turn events. Commit 3 `3bae14b feat(memory): rank local recall with audit metadata` cherry-picks Claude worker `f386668` and adds ranked local confirmed-memory recall, `lastMemoryRecall`, and Mem0 fallback preservation. `OpeningPage.vue`, `Experience.vue`, and `MemoryIndicator.vue` untouched. Verification: focused runtime/context/memory suite 5 files / 66 tests passed; full `npm run test:run` passed 111 files / 802 tests; `npm run build` passed; `git diff --check` clean. External Claude CLI workflow documented in `AGENTS.md`; persistent Codex memory written to `/home/recoletas/.codex/memories/claude_parallel_workflow.md`. Remaining worktree noise: untracked `docs/demo/n5c-material-page-20260617_002.png` pre-existed and was not touched.
<!--
  Keep the latest 5-10 completed handoffs, newest first.
  Remove entries once they are reflected in docs/LOG.md, docs/PLAN.md, known issues, or commits.
-->
- 2026-06-17 17:30 CST — Claude on `feat/writing-kao-grammar` (worktree `/home/recoletas/worktrees/writing-kao`, commits `f87d4a9` + `4200da8` + `0bf2f48`): W3 round 2 polish — 3 low-severity review fixes. (1) Narrow `.theme-kao .folio-surface--paper` → `.theme-kao .writing-page .folio-surface--paper` to stop z-index leak to `Experience.vue:60` quick-note-workspace-header-wrap. (2) Strengthen chapter list focus ring from 1px → 2px solid gold + 4px gold-tint 30% outer glow (WCAG 2.4.7 "highly visible" — 1px was at minimum). (3) Make `kickerPulse` keyframe visible: was animating `text-decoration-color` which is a no-op because `BookmarkButton.vue:106` has `text-decoration: none`; changed to animate `box-shadow` (4px/30% → 6px/50% outer gold-glow breath), aligned with Fix 2's static 2px+4px baseline for seamless animation. 1 new uiPolish test (keyframe body assertion); test count: 60/60 uiPolish, 70/70 4-contract, 109/775 test:run, build clean, diff:check clean, 0 Co-Authored-By. Plan: [`docs/superpowers/plans/2026-06-17-writing-w3-emergence.md`](./superpowers/plans/2026-06-17-writing-w3-emergence.md).
- 2026-06-17 17:10 CST — Claude on `feat/writing-kao-grammar` (worktree `/home/recoletas/worktrees/writing-kao`, commit `7b30b81`): W3 visual emergence commit 3 (final) — chapter list motion. Hover/focus only kickerPulse 1.5s on .chapter-list-item .bookmark-button:hover/focus/focus-visible with 1px --archive-gold hairline. Reuses 5B-ship @keyframes kickerPulse. Scoped to .chapter-list-item so it doesn't leak to other BookmarkButton consumers. 2 new uiPolish contracts green; 4-contract gate 66/66 green; full test:run 109 files / 771 tests +0 regression; build clean; diff:check clean. Manual screenshot of /writing reviewed pre-commit. **W3 3 commit ship gate complete** (drop-cap + 3-plane+wallpaperMist+titleGlow + chapter list motion). Plan: [`docs/superpowers/plans/2026-06-17-writing-w3-emergence.md`](./superpowers/plans/2026-06-17-writing-w3-emergence.md). Branch NOT pushed.
- 2026-06-17 15:50 CST — Claude on `feat/writing-kao-grammar` (worktree `/home/recoletas/worktrees/writing-kao`, commit `0de4b68`): W3 visual emergence commit 2 — 3-plane z-axis + wallpaperMist + titleGlow, with prerequisite keyframe self-containment fix rolled into the same commit (was originally intended as the `feat(writing)` commit 2; code review discovered the `wallpaperMist` / `titleGlow` / `kickerPulse` `@keyframes` are NOT in `main.css` but in `CharacterBackdrop.vue` + `OpeningPage.vue` (neither mounted on `/writing`), so the fix copied identical keyframe definitions into kao.css + added 3 uiPolish contracts to lock the new location, then committed as `fix(writing)` to keep the prerequisite fix together with the consumers it enables). 5 new `.theme-kao` rules in kao.css: back/window/front z-axis using `--z-stage-decor` / `--z-stage-hero` / `--z-stage-cta` tokens (existing main.css:7-9 from 5A); `wallpaperMist` 14s olive gradient breath on `.editor-container::before`; `titleGlow` 4.8s on `.chapter-title-input` (28px, letter-spacing 0.04em, `font-family: var(--font-display)` LXGW WenKai). 7 new uiPolish contracts in the W3 describe block (3-plane z-axis × 3 + `wallpaperMist` consumer + 3 keyframe self-containment locks). 4-contract gate 67/67 green; full `npm run test:run` 109 files / 772 tests +0 regression; build clean; `git diff --check` clean. Manual screenshot of `/writing` reviewed pre-commit. Plan: [`docs/superpowers/plans/2026-06-17-writing-w3-emergence.md`](./superpowers/plans/2026-06-17-writing-w3-emergence.md). Spec: [`docs/superpowers/specs/2026-06-17-writing-w3-emergence-design.md`](./superpowers/specs/2026-06-17-writing-w3-emergence-design.md). Branch NOT pushed.
- 2026-06-17 15:38 CST — Claude on `feat/writing-kao-grammar` (worktree `/home/recoletas/worktrees/writing-kao`, commit `70bb601`): W3 visual emergence commit 1 — drop-cap. First character of first paragraph in editor preview becomes 3-line LXGW WenKai gold-to-rose gradient initial via `.theme-kao .editor-preview > p:first-of-type::first-letter`. CSS-only change in kao.css; 0 template change to Writing.vue; 0 new components; 0 new deps. 3 new uiPolish contracts green; 4-contract gate green; full 109/765 test:run pass; build clean; diff:check clean. Reduced-motion a11y foundation block established in kao.css (commits 2/3 extend selector list). Plan: [`docs/superpowers/plans/2026-06-17-writing-w3-emergence.md`](./superpowers/plans/2026-06-17-writing-w3-emergence.md). Spec: [`docs/superpowers/specs/2026-06-17-writing-w3-emergence-design.md`](./superpowers/specs/2026-06-17-writing-w3-emergence-design.md). Manual screenshot of `/writing` reviewed pre-commit. Branch NOT pushed.
- 2026-06-17 14:55 CST — Claude on `feat/writing-kao-grammar` (worktree `/home/recoletas/worktrees/writing-kao`, commit `a3b650b` v2 amend): Writing page kao archive-folio surface ship via 1 atomic commit (W1 + W2 + W4 bundled) + v2 审查修复 amend (5 真 bug + 4 dead CSS + 3 doc 错)。Scope: W1 (FolioSurface wraps 4 zones: `WorkbenchPageHero` as="header" chrome+plain v2 改 decorated=false 防双层 chrome 框, `books-sidebar` as="aside" paper+decorated, `editor-main` as="main" chrome+plain, asset-inbox modal as="article" paper+decorated) + W2 (chapter list rows are `<BookmarkButton variant="tertiary" size="compact" :index :label>` consuming `selectChapter(chapter.id)`; AI panel primary 应用/secondary 取消 are `<BookmarkButton variant="primary|secondary">` 配 v2 `.ai-result-actions { display: grid; grid-template-columns: 1fr 1fr }` 防 144px 垂直堆叠; mode switch stays `.action-btn` per `stereo-migration-design.md:428-432` lock) + W4 (CharacterPortrait pose-id="writing-sidekick" size="thumb" caption="批注中" v2 加 `style="max-width: 180px"` 防 210px 侧栏溢出 + `v-show="!isRightCollapsed"` 守卫防 44px 收起时漏出; pose registered in `characterArt.js` as status="stub" pointing at `stub-silhouette.svg`). v2 kao.css 加 8 条 `.theme-kao` gated 规则(`.books-sidebar { display: flex; flex-direction: column }` 救 scoped CSS 不穿透 FolioSurface 根 `<aside>` / `.sidebar-header` transparent + padding-top 32px 防撕角 clip / `.ai-panel` 重命名自死 `.writing-ai-panel` / `.asset-inbox-modal` transparent + header padding-top 32px / `.bookmark-button.active` gold ring 救章节选中无视觉反馈)。10 个新 uiPolish + 2 个新 stereoMigration(含 characterArt 6→7 + useCharacterArt writing-sidekick 验证 + 3 个 v2 review-fix contracts: footer v-show / .active 规则 / .books-sidebar flex 救活)。**Verification**: `npm run test:run` 109 files / 762 tests +0 regression; 4-contract gate (uiPolish + welcomeView + workbenchNav + themeVariantView) 57/57 green; `npm run build` clean (Writing JS 68.24 kB / gz 26.05 kB (v2 vs v1 +50 B raw, gz 持平)); `git diff --check` clean; no `Co-Authored-By` footer。**Do-not-touch preserved**: `gameStore.js` / `worldbookContextBuilder.js` / `generation*` / `StatusBar.vue` / `useCharacterArt.js` / `components/folio/*` 0 改动。**Deferred** to follow-up commits: W3 (立体感 3-plane + drop-cap + wallpaperMist + titleGlow for editor surface), Tiptap v3 migration, Codex 右侧栏 (Tier 2 #15), 5B v0.2 real art for writing-sidekick, Notes.vue + ProseEssay.vue Phase 1C rollout, CharacterPortrait 缺 `compact` size 内置(当前 inline `max-width: 180px` 约束)。Plan: [`docs/superpowers/plans/2026-06-17-writing-kao-grammar.md`](./superpowers/plans/2026-06-17-writing-kao-grammar.md)。Branch NOT pushed (user typically reviews commit first)。
- 2026-06-17 09:04 CST — Codex on `main`: investigated `/opening` apparent rollback. Root cause was not CSS tuning: reflog showed multiple `git reset` operations at ~08:39 CST plus a checkout to `tier1-gated-test`, rebase abort, and checkout back to `main`, which wiped the uncommitted direct.png alignment edits from the worktree. Also Vite logs showed a transient `src/styles/main.css` conflict marker error (`<<<<<<< HEAD`) during that period, so browser HMR could have stayed on stale compiled CSS. Re-applied the direct-aligned `/opening` pass on current `main`: embedded per-glyph serif title, left copy dropped onto the window plane, lower-left foreground shard buttons at the red guide area, right-bottom scene strip pushed toward/off the right edge, and tests re-synced from reset-old contracts (v310/v311/v312/v313/v37/faceRotation/openingFullBleed/openingPageVisualFit/uiPolish subset). Verification: opening contract group passed (11 files / 64 tests); `uiPolish` opening subset passed (1 file / 15 tests, 24 skipped); `npm run verify:post` passed (build OK + diff clean, existing `kao.css` static+dynamic import warning remains). Restarted Vite cleanly; current preview is `http://127.0.0.1:5174/opening` (do not use stale 5173 for this pass).
- 2026-06-16 21:30 CST — Claude on `main`: 5A + 5B v0.1 + 5C v3.14 ship via `feat/5b-stereo-art` (single local commit, **NOT pushed** — user 16:30 wants to review state first). Theme-system preserved per user 18:30 instruction. **Why combined 1 commit**: 5A/5B/5C are the same feature (立体感 migration ship gate) split across 3 feature branches; landing them atomically keeps the user-tuned opening page contract intact. **What landed**: 5A stereo migration (3 atomic commits from `feat/5a-stereo-migration`: CharacterPortrait + CharacterArchiveStrip stub + useCharacterArt composable + stub silhouette svg) + 5B v0.1 (6 micu gpt-image-2 webp portraits at `src/assets/characters/kao-archive-*.webp` 144-182 KB each, characterArt.js 6 import entries) + 5C v3.14 (the latest user-tuned state: italic serif title (Iowan Old Style + skewX(-4deg)), 10s ease-in-out premium titleShimmer (v3.12 was 4s linear), wallpaperMist/wallpaperGrain/wallpaperLight keyframes replace v3.12 artBreathe/artDrift/titleShimmer, ZCOOL XiaoWei for both --font-display and --font-serif after v3.13 QingKe HuangYou artification was reverted per user "字体有点奇怪" feedback) + theme-system (kao.css + 4 legacy snapshots + AppearanceControls + themeStore preserved) + source fixes (legacy.css recreated per theme-system contract, index.html ZCOOL XiaoWei Google Fonts preconnect + stylesheet, main.css is-archive-* utilities removed since 5b HEAD moved them to kao.css gated by .theme-kao). **5C v3.14 contract supersedes v3.12**: 5b worktree had v3.11 → v3.12 → v3.13 (QingKe HuangYou artification) → v3.14 (revert artification + slow premium shimmer) iterated on top of v3.10; user 21:00 said "5173 的够了" picking v3.14 as the tuned state to land on main. **Known test failures (19 tests, 9 files, pre-existing 5b HEAD contract drift, not new from this commit)** — v3.14 source removed artBreathe/artDrift/titleShimmer but v310/v311/v312/v313 still expect them; v3.14 italic title replaces var(--font-display) shimmer but uiPolish LXGW test still expects var(--font-display); v3.14 is-archive-* lives in kao.css (gated) but uiPolish "exposes in main.css" still expects them in main.css; v3.14 .stage-command is LARGE 210x72 but uiPolish pass 4 still expects micro 40px; ctaSlant walks src/pages/legacy/ which has -3deg/+2deg snapshot tils from 4e779d2 boundary; v37 expects 8px notch but v3.14 has 14px; v39b expects artBreathe; faceRotation expects .character-backdrop__face-region. These tests need a follow-up commit to update contracts to v3.14 reality (or be marked .skip with documented reasons). **Verification**: `npm run test:run` 9 files / 19 tests / 720 pass / build clean / diff clean. **5b worktree / branch status**: `feat/5b-stereo-art` has 4 unpushed commits (v3.10, v3.11, v3.12, v3.13, v3.14) on top of origin. After this commit, `git diff main feat/5b-stereo-art` is 0 (5b is the linear ancestor of v3.14, now identical to main HEAD content). **5b worktree + branch cleanup pending** per user 21:00 ("5175 那个分支如果只有这两个不一样的话就可以删了" — 5b has 4 unpushed commits not 2, user may want to delete the worktree + branch and lose the v3.10-14 history; awaiting user confirmation before destructive action).
- 2026-06-16 18:56 CST — Codex on `main`: reviewed and optimized [docs/superpowers/plans/2026-06-16-worldbook-token-save.md](./superpowers/plans/2026-06-16-worldbook-token-save.md) into v2 against current `main` code, not just text/grammar. Key feasibility fixes: V2 worldbook sorting no longer uses the broken `priority !== type-default` detector; plan now matches current `keysSecondary` / `entry.injection.mode` code shape; removed unnecessary `buildWorldbookContextV2()` API; token budgeting now uses shared `estimateTokens()` with CJK/ASCII/other buckets instead of blunt `tokenBudget * 1.3`; gameStore debounce plan now preserves the Options-store public `saveSessions()` action via per-store `WeakMap` debouncers and ESM tests. Added `AGENTS.md` review-expectation rule: spec/plan/doc review must cross-check feasibility against repo state and patch docs when user asks "审查优化". Also wrote Codex memory `/home/recoletas/.codex/memories/review_expectations.md` with the same preference. Verification: agent-maintenance symlink/frontmatter checks passed; `git diff --check -- AGENTS.md docs/STATUS.md docs/superpowers/plans/2026-06-16-worldbook-token-save.md` passed; `npm run docs:build` passed; `npm run build` passed with existing theme-system warning (`kao.css` static+dynamic import); `npm run verify:full` currently fails before docs/build on existing theme-system contract gap: missing `src/styles/themes/legacy.css` causes 4 failures in `legacySnapshot.test.js` / `uiPolish.test.js`.
- 2026-06-16 15:10 CST — Claude on `main`: theme-system merged from `feat/theme-system-20260615` → `main` (merge commit `d1b2316`, --no-ff, 47 commits squashed-in, **zero conflicts**). Pre-merge: local main was 4 commits behind `origin/main` (PR #1 `wip/map-realism-render-docs-20260608` work); ran `git pull origin main` first → synced to `1898a25`, then `git merge --no-ff feat/theme-system-20260615`. Theme-system internals unchanged — same 11 v1 atomic commits + 6 v1.1 fixups + 6 v1.2 fixups as on the feature branch. 47 commits ahead of pre-merge `main` (3b3c090). Conflicts with `feat/5b-stereo-art` (same merge-base, overlapping `useTheme.js`/`OpeningPage.vue`/`main.css`/`router/index.js`) DO NOT materialize now because 5B content is NOT on `main`; when 5B eventually lands it must rebase onto main (with theme-system) and resolve those overlaps then. **Verification**:`npm run test:run` 91 files / 665 tests pass; `npm run build` clean (3.76s); `git diff --check` clean. Working tree clean except 5 untracked `pinax-5c-*` visual-audit files (unrelated to theme-system). Other branches left untouched per user instruction: `feat/v-memo-on-list` `feat/tier1-quickwins` `feat/tier1-medium` `feat/5a-stereo-migration` `feat/5b-stereo-art` all stay on their current state pending separate review.
- 2026-06-15 18:55 CST — Claude on `feat/theme-system-20260615`: theme-system v1 implementation handoff (7 atomic commits, follow spec `docs/superpowers/specs/2026-06-15-theme-system-design.md` v2 + plan `docs/superpowers/plans/2026-06-15-theme-system.md`). 6 文件 legacy snapshot 从 `4e779d2` 抽出 + Pinia `themeStore` (variant + colorScheme + localStorage `app_theme_variant` + 保留 `app_theme`) + `useTheme` shim 保 back-compat + `main.css` 拆 base + `themes/{kao,legacy}.css` (Vite dynamic import，selector gated by `.theme-kao/.theme-legacy`) + `index.html` LXGW 静态 preload 删除 + `ThemeVariantView` / `ThemeAssets` wrappers + `router/index.js` 3 路由过 wrapper + `AppearanceControls` 4-radio 进 AppShell 抽屉 + LXGW kao 动态 preload + `useTheme()` 调用点 (WelcomeView/OpeningPage/Writing/Notes/ProseEssay/StructuredSettings) 0 破坏 + `uiPolish` +13 静态契约 / `themeStore` 8 tests / `themeVariantView` 3 smoke. 全量验证:`npm run test:run` 通过（应 87+ files, 600+ tests）；`npm run build` 通过；`git diff --check` clean。Out of scope: 立体感 migration interplay / build-time variant flag / per-page override / URL ?theme= override / Playwright visual（环境无 Chromium 维持 deferred）。
- 2026-06-15 18:30 CST — Claude on `feat/5b-stereo-art` (5B ship gate, worktree `/home/recoletas/jiuguan/worktrees/5b-art`): 5B v0.1 ship via **micu placeholder flow** (user 18:30 改主意: image_generate 质量 OK for v0.1, 自己画 v0.2 再说). **2 commits on `feat/5b-stereo-art`**:`b9ccb39` `feat(art): swap stub silhouettes for 6 micu kao-archive portraits (5B v0.1 placeholder)` (14 files: 6 webp in `src/assets/characters/kao-archive-*.webp` 144-182 KB each + 6 PNG 留档 in `docs/demo/kao-archive-*-001.png` + `src/config/characterArt.js` 6 import + 6 status: stub→real + `src/__tests__/stereoMigration.test.js` contract evolve 5A stub→5B real) + `34c7754` `fix(process-inbox): avoid self-cp when input is already $pose.png` (1 line, 1 file; ship-gate 跑时被发现, 修了 ship + future user-draw 都会 hit 同一 bug). 6 立绘 (3 char + 3 scene) 走 micu gpt-image-2 (1K 档 1024×1360 spec → 实际 1088×1445 = 1.57MP 福利档, 米醋等比放大), palette/构图/撕角全 spec 命中 (单张微漏 tiara red gem + mole 细节, 占位质量不影响 ship). **Verify**:`npm run test:run` 通过 (88 files, **625 tests** +0 regression);`npm run build` 通过 (3.76s);`git diff --check --cached` clean. **Deviations from plan** (logged): (1) art source = micu 而非 user-draw (per user 18:30 决定, memory `feedback_no_uninvited_image_gen` 例外: user 显式 "run" 即放行); (2) 5b 分支仍 off `feat/5a-stereo-migration` (5A PR not yet merged), 待用户合 5A PR → `git fetch origin main && git rebase origin/main` in 5b worktree。**PR 链接** https://github.com/Recoletas/Pinax/pull/new/feat/5b-stereo-art — user 可 review + merge。Prep docs (`5b-prompts.md` / `draft-mode.md` / `ship-gate.md` / `process-inbox.sh`) 仍为 5B v0.2 hand-draw 替换的有效 brief,未改。
- 2026-06-15 16:30 CST — Claude on `wip/map-realism-render-docs-20260608`: 综合报告 v3 修订 + 起 spec 落盘。**v3 修清单 (本轮第 4 次 review)**:**1 项 v2 误判 + 2 项数字偏差 + 1 项表头错误**——(1) Tier 1 #10 "立体感 v5 plan doc 落地"误判为缺失,实则 `docs/superpowers/plans/2026-06-15-stereo-migration.md` 51 KB / 1164 行 / 2026-06-15 09:24 写就,本条从 Tier 1 删除,§1.10 / §2.2 spec table / §5.1 / §5.4 / §6 Q3 / §8 retention 全部划线标注;(2-3) "3 过期 spec" 年龄偏差(mem0-cors 11d / pinax-homepage 6d[v2 误 18d] / realistic-tectonics 7d[v2 误 12d] / internal-agent-workflow 7d),Tier 1 数 = 11(v2 误 12),"10 不需要 do-not-touch" 改 "9 不需要";(4) §8 retention summary 同步。**v3 加 §0.6 v2→v3 修正表**(4 行,见报告)。**新 spec 落盘** [docs/superpowers/specs/2026-06-15-list-vmemo-design.md](./superpowers/specs/2026-06-15-list-vmemo-design.md) (Draft v1, 218 行,沿 5A design 模板): 综合报告 Tier 1 #1 `v-memo on list` 转化 — 4 落点 (QuestLog.vue L158 activity / L240 relations / MemoryIndicator.vue L101 visibleCandidates / ImageGenRail.vue L93 imageLibrary) + 1 uiPolish contract test + 4 场景 manual perf 验收 + 1 commit `perf(list): v-memo 4 high-churn v-for blocks ...`,zero risk,no new deps,无 do-not-touch 冲突,5A 5B worktree 路径隔离;4 Open Questions 留 user 拍(spec 留 4 选项:deps 单测覆盖 / 是否同时升级 v2 综合 / 是否扫更广 list / 是否进 plan)。综合 v3 文件同路径覆盖 [docs/plan/comprehensive-research-synthesis-20260615.md](./plan/comprehensive-research-synthesis-20260615.md)。未改代码 / 未跑测试 / 未起 plan — 等 v-memo spec user review 批准后再进 writing-plans 流程。
- 2026-06-15 15:55 CST — Claude on `feat/5a-stereo-migration` (5A 起点 — worktree `/home/recoletas/jiuguan/worktrees/5a-stereo`): 5A 立体感骨架 + stub 落地 3 atomic commits。**新增 5 文件** (`src/assets/characters/stub-silhouette.svg` 22 lines + `src/config/characterArt.js` 10 lines / 6 entries + `src/composables/useCharacterArt.js` 15 lines + `src/components/folio/CharacterPortrait.vue` 55 lines + `src/components/folio/CharacterArchiveStrip.vue` 47 lines) + **`src/__tests__/stereoMigration.test.js`** 6 tests (2 module shape + 4 page wiring)。**`src/pages/OpeningPage.vue`** 4 sites 接入 (`<CharacterPortrait pose-id="opening-cover" size="hero">` inside `<div class="opening-stage-poster">` wrapper 保留 3-property 锁;`<ArchiveStrip>` → `<CharacterArchiveStrip :tiles="openingActionStubTiles">`;`openingActionStubTiles` computed 3 scene tiles;`--hero-image` CSS var 绑 `useCharacterArt().resolveArt({ poseId: 'opening-cover' }).src`)。**`src/pages/Experience.vue`** 3 sites 接入 (`<FolioSurface as="div" variant="chrome">` wrap `<div class="inline-detail-card">` + `<CharacterPortrait pose-id="speaker-thumb">` in header;`<FolioSurface as="div" variant="paper">` nested wrap `<header class="quick-note-workspace-header">`;`.quick-note-panel-btn` × 6 → `.action-btn` / `.action-btn.primary`)。**`src/pages/OpeningPage.vue` CSS** 删 15 dead child rules (-166 lines),保留 `.opening-stage-poster` 主规则作 thin alias(3 锁属性 `isolation: isolate` + `mix-blend-mode: multiply` + `background-image: var(--hero-image, ...)` 完整)。**2 段 review 全过**:spec compliance ✅,code quality ✅ (Ready to merge)。**3 atomic commits**:`d5d4291` feat(folio) modules → `5d107f9` feat(pages) wiring + 2 Important code quality fixes → `0a98d91` refactor(opening) css;**全部无 Co-Authored-By footer**。**Pre-Task 2 修 3 处 user-found issues** (HIGH: `docs/demo/kao.jpg` tracked `3567fee` 解 fresh worktree build 失败 / MEDIUM: `.git/info/exclude` 加 `node_modules` 解 worktree symlink untracked 噪声 / LOW: `CharacterPortrait` stub tape 加 `is-archive-prop is-archive-prop--tape` 复用 main.css 既有 utility)。**全量验证**:`npm run test:run` 通过 (88 files, **625 tests** +0 regression);`npm run test:run -- src/__tests__/stereoMigration.test.js src/__tests__/uiPolish.test.js src/__tests__/welcomeView.test.js src/__tests__/workbenchNav.test.js` 通过 (**51 tests** = 6+39+4+2);`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests);`npm run build` 通过 (3.78s);`git diff --check` clean。Spec: [docs/superpowers/specs/2026-06-15-stereo-migration-design.md](./superpowers/specs/2026-06-15-stereo-migration-design.md) (v5 `d140f00`);Plan: [docs/superpowers/plans/2026-06-15-stereo-migration.md](./superpowers/plans/2026-06-15-stereo-migration.md) (Task 0–4 全完成,Task 5–13 待 5B 启动)。分支 `feat/5a-stereo-migration` 已 `git push -u origin` 推到 GitHub,**PR pending** — 等用户合 PR 后启 5B worktree (`feat/5b-stereo-art`) 跑 6 image_generate + cwebp + per-pose status flip。
- 2026-06-15 15:30 CST — Claude on `wip/map-realism-render-docs-20260608`: 用户要求"不惜一切 token 代价,扫描整个项目,进行一切方向的市场调研,提出所有可能的优化点",派 12 个并行子 agent 综合扫描 + 市场调研(4 份内部只读审计 + 8 份外部市场调研),零文件改动(全程 read-only)。**全部 12 份报告就位**,综合报告 v1 落盘 [docs/plan/comprehensive-research-synthesis-20260615.md](./plan/comprehensive-research-synthesis-20260615.md)。**v2 修订 (15:55 CST)**:用户要求"多审查优化几遍",再派 3 个并行子 agent 复核(citation accuracy / coverage gap / priority validation),逐条 grep 验证 50 处 file:line 引用,纠错 8 处 / 修正 10 处 / 显式归档到 Tier 5 "v1 误判需纠正"。Tier 1 重洗牌:5 项降级(graphology 1-3 天→1-2 周 / VueUse 13 是 sweep / Worldbook Codex 合并 Tiptap / 营销 v5 冲突 / Vitest 4 caveat)、3 项升级(v-memo on list / AppShell focus trap / color contrast)、6 项新增(立体感 v5 plan doc 补 STATUS.md In flight 缺口 / markRaw LLM 响应 / vite-plugin-pwa sub-scope / localStorage quota 监控 / /readyz / mem0-cors spec header)。Tier 2 增 7 项(i18n / First Project seed / ecosystem.config deterministic + 部署 runbook / currentScene+sceneState token / effectScope per session / Vector RAG spike / inspectable save)。Tier 3 增 4 项(perf SLO / pre-commit hook fails on console.error / a11y scaffolding skip-link+reduced-motion+aria-live / GitHub Actions matrix)。**净增 14 个优化点**到 74,跨 11 个之前未覆盖领域。**显式 Tier 5**:8 项 v1 错误主张归档(Express 9→8 routes / graphology+d3-hierarchy 0 引用→d3-hierarchy 1 处 / Mem0 删 v1→实际裸 URL / 88→87 test files / ecosystem.config 字节相同→md5 不同 / pass2-screenshots 不存在→存在 6 PNG / 4 未 track→3 / duplicate spin at main.css:34,488→AppShell.vue:488 为主重复)。**未改代码 / 未跑测试 / 未起 spec/plan**——纯研究交付。Tier-1 转 spec 候选(v2 重新排序):v-memo on list + Mem0 删除路径 + /healthz+/readyz + 404 + 3 过期 spec + useExperienceStore 删除 + AppShell focus trap + color contrast + Vue 3.5 + 立体感 v5 plan doc + (do-not-touch exception gated)V2 世界书 + CJK token + save debounce bundle + Pinia 3 + markRaw LLM 响应。所有交付文件(15 份,~340 KB):[ai-writing-assistants-research-20260615.md](./plan/ai-writing-assistants-research-20260615.md) (43.7 KB) / [if-engines-market-research-20260615.md](./plan/if-engines-market-research-20260615.md) (44.8 KB) / [worldbuilding-platforms-research.md](./plan/worldbuilding-platforms-research.md) (25.0 KB) / [worldbook-market-research-20260615.md](./plan/worldbook-market-research-20260615.md) (37.1 KB) / [block-editors-research-20260615.md](./plan/block-editors-research-20260615.md) (31.5 KB) / [local-first-sync-research-20260615.md](./plan/local-first-sync-research-20260615.md) (28.7 KB) / [map-rendering-libs-research-20260615.md](./plan/map-rendering-libs-research-20260615.md) (27.6 KB) / [vue3-ecosystem-research-20260615.md](./plan/vue3-ecosystem-research-20260615.md) (22.9 KB) / [comprehensive-research-synthesis-20260615.md](./plan/comprehensive-research-synthesis-20260615.md) (本综合 v2, 65k 字)。
- 2026-06-15 12:30 CST — Claude on `wip/map-realism-render-docs-20260608`: 完成 local-first 存储与 sync 调研 [docs/plan/local-first-sync-research-20260615.md](./plan/local-first-sync-research-20260615.md)。覆盖 11 个候选（RxDB / PouchDB / Dexie.js / Triplit / PowerSync / Yjs / Automerge / Tinybase / LiveStore / Replicache / ElectricSQL），8 节正文（DB 对比 / CRDT / 同步模式 / 迁移 / Vue 集成 / bundle / 推荐 / 引用），每条 claim 配 URL。**Live-verification gap：** Firecrawl MCP (HTTP 401) / WebSearch (400 invalid params) / WebFetch (domain safety block) 在本 session 均不可用，全部事实点来自 Context7 对各库 canonical 路径的实时文档拉取；bundle size 与 current version 在数据不可达处标 `n/a (no live data)` 而非凭印象写。**结论：** Pinax 现在的 localStorage + 25+ writes/session 痛点应在不引新依赖的前提下先做 schema 版本 + IndexedDB + 批写（Phase A/B）；RxDB 是 multi-device sync 的最贴切候选，Yjs + Hocuspocus 留给真要做协同编辑的 Phase D，PouchDB / PowerSync / LiveStore / ElectricSQL 列为 defer 或 no。Phase A/B/C/D 4 阶段迁移路径在文档 §7。**未改代码 / 未改测试**——本文是研究交付，与之配套的落地 spec/plan 应单独立项。
- 2026-06-12 19:00 CST — Claude on `wip/map-realism-render-docs-20260608`: 按用户“左边最大的世界书字体我打算换成艺术字，全面参考调研给我建议”落地 LXGW 霞鹜文楷真艺术字方案。新增 `src/assets/fonts/LXGWWenKai-Regular.woff2` (593 KB, OFL 1.1, GB2312 常用 2700 字 + ASCII + CJK 标点子集) + `OFL.txt`；`src/styles/main.css` 加 `@font-face` for `LXGW WenKai` 与 `--font-display` / `--font-serif` / `--font-sans` / `--font-mono` 四个 token；`index.html` 加 `rel="preload" as="font" type="font/woff2" crossorigin` 让 hero 标题零 FOUT；`src/pages/OpeningPage.vue` 把 `.opening-title-block strong` 从 `Iowan Old Style` 栈切到 `var(--font-display)` 并把 `font-weight: 900` 降到 `400`、letter-spacing 提到 `0.04em`、text-shadow 加一道 `--archive-paper` 高光——LXGW 自身已是书法体，再叠 900 字重反而糊。其余 9 处 `Iowan Old Style` 栈保留走 `var(--font-serif)` 语义（同 main.css 变量）。新增 `uiPolish` 契约 5 条：font-face / 4 token / preload / OpeningPage 用 `var(--font-display)` / 字体文件存在 + < 1 MB。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 39 tests，+5 新增)；`npm run build` 通过 (Vite 自动把 `/src/assets/fonts/...` preload 重写为 hashed `/assets/LXGWWenKai-Regular-DNTqAPtM.woff2` 593 KB，未触发 600 KB chunkSizeWarningLimit 警告)；`npm run test:run` 通过 (87 files, 619 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)。未做 Playwright 视觉验证 (环境无 Chromium/Playwright，Spec R11 safety net 维持 deferred)。字体子集化时遇到 cmap `in` 误判坑 (`cmap.getBestCmap()` 返回 dict-like 含 defaultdict 行为，必须用 `.get(cp) is not None` 严判)，已绕开。霓/晦 等冷僻字未进子集，运行时回退到 `Songti SC` / `STSong` / `Noto Serif CJK SC` 链路，浏览器自动接管。
- 2026-06-12 18:53 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户继续反馈“原本体验页的这部分没删”，完成 `/experience` 残留开场/入口 folio 的二次拆除：`Experience.vue` 顶部 `experience-stage-band` / `playable-world-strip` 整段不再渲染，旧的 `进入世界` / `会话` BookmarkButton、现场摘要、开场钩子、poster/archive strip、`startWorldAdventure()` / `ensureWorldAdventureSession()` / `getLatestSessionForWorldbook()` opening 会话链路均从 `/experience` 移出，并清理 `Experience.vue` scoped CSS 中旧 `experience-stage` / `playable-world` / `stage-command` / `opening-*` selector 残留；`/experience` 现在只保留当前冒险会话工作区、侧栏、速记/顾问/机制等工作态 chrome，`/opening` 继续独占世界选择、开场摘要和 `开局` / `改写` hidden GM 指令。更新 `uiPolish` 契约锁定 `/experience` 不再拥有开场页/入口按钮，opening poster 和 micro stage-command 规则归属 `OpeningPage.vue`。验证：先跑红灯确认旧残留触发 9 个失败；实现后 `npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/welcomeView.test.js src/__tests__/workbenchNav.test.js` 通过 (3 files, 40 tests)；`npm run build` 通过；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`npm run test:run` 通过 (87 files, 614 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)；`git diff --check` 通过；`git diff --check --no-index /dev/null src/pages/OpeningPage.vue` 无 whitespace 输出（exit 1 为 no-index 内容差异预期）。
- 2026-06-12 18:38 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户澄清“脱离 `/experience` 路由”完成开场页路由拆分：新增独立 `/opening` route / `src/pages/OpeningPage.vue`，承接原开场页的 archive-folio 视觉、开场钩子、现场 / 阻力 / 出口摘要、`开局` / `改写` 按钮和 hidden GM 指令发送；`/opening` 自己读取 playable world entry intent、设置 active worldbook、创建/恢复会话并在发送 hidden action 后跳回 `/experience`。`/experience` 删除 `playable-world-opening-page` 和 opening intent 消费逻辑，不再在 mounted / worldbook change 中因开场 intent 自动创建会话；左页 `进入世界` 改为跳 `opening`。欢迎页主入口改 `/opening`，世界选择页 `enterPresetWorld()` 改为保存 intent 后跳 `opening`，工作台体验默认路由和侧栏增加 `开场页`。更新 `welcomeView` / `workbenchNav` / `uiPolish` 契约锁定该分工。验证：先跑红灯确认旧入口/缺新页失败；实现后 `npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/welcomeView.test.js src/__tests__/workbenchNav.test.js` 通过 (3 files, 40 tests)；`npm run build` 通过；`npm run test:run` 通过 (87 files, 614 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`git diff --check` 通过。
- 2026-06-12 18:15 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户“把这个页面独立出来”继续收 `/experience` 开场页：把原本只剩 header 的 `playable-world-scene-sheet` 改为独立 `playable-world-opening-page` section，保留 `开局` / `改写` hidden GM 指令链路，同时补回页面自身所需的开场钩子、现场 / 阻力 / 出口摘要；新增 `playableWorldOpeningFacts` 专供该独立页使用，不恢复旧的 `opening-scene-grid` / `opening-brief-lines` / `opening-action-directory` / GM 指令预览。更新 `uiPolish` 契约锁定独立页结构。验证：先确认新增契约红灯（缺 `playable-world-opening-page`），实现后 `npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run test:run` 通过 (87 files, 614 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)；`npm run build` 通过；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`git diff --check` 通过。
- 2026-06-12 16:03 CST — Codex on `wip/map-realism-render-docs-20260608`: 修正上一轮对 `/experience` 反馈的误读：不再用 `showExperienceEntryStage` / `currentSession` 隐藏整个顶部体验入口；保留默认体验面板、左侧 `进入世界` / `会话` 和右页 `开局` / `改写`。本轮实际删除的是用户点名的右页冗余现场索引块：`playable-world-route`、`opening-scene-grid`、`opening-action-directory` 及其 `切口目录 / 第一异常 / 01-03` 列表样式残留。`quick-notes-rail`、`GmPersonaLauncher`、`game-image-gen-rail` 继续只在已有用户输入后出现；`MemoryIndicator` 在 `/settings/worldbook` 或 `/experience` 开场过渡态隐藏。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`npm run build` 通过；`git diff --check` 通过。未跑全量 test。

## Next up
<!--
  Things planned but not yet started. Move into "In flight" when picked up.
-->
- Next decision after usability ship: pause broad UI polish until user chooses the next lane: (A) return to a Phase 1 design-bible pass, or (B) run real hand-test usability validation on the current shell/activity flow and convert findings into one larger product slice. Require any Claude worker reports/screenshots to be written to disk; keep reports capped and avoid multi-round micro-polish unless there is a blocker.
- 5B v0.2: user 手画终稿覆写 v0.1 micu 占位。同名 webp 覆盖 `5b-art/inbox/kao-archive-{pose}.png` → `./process-inbox.sh` → 再 ship 一次 (process-inbox.sh bug 已 34c7754 修)。Briefs 仍有效:`/home/recoletas/jiuguan/worktrees/5b-art/5b-prompts.md`。
- 5A PR merge → 5b rebase:`git fetch origin main && git rebase origin/main` (in 5b worktree). 5A 3 commits 应 fast-forward 干净。
- UI shell 线程：按 `character-driven` §3.4 / §3.5 + [superpowers/specs/2026-06-10-ui-redesign-design.md](./superpowers/specs/2026-06-10-ui-redesign-design.md) 继续 Phase 1B，先稳住 `WelcomeView` 的 olive/gold kao 档案册首页(3 阶段路径：CSS tokens + 自包含 utility → 4 kao 组件 + 模板局部重排 → 3 文件复用 + 1 次人工截图)，再统一 `AppShell` 的 hidden-first chrome；在真实位图资产接入前，不再使用 CSS 假人物 / 假剪影；不提前滑进 Motion One / GSAP。
- UI shell 线程：在首页和 chrome 稳定后，再推进 Phase 1C，把 `Experience / Writing / Notes / ProseEssay` 分别对齐到正立对话态、侧视静默态和编辑态的在场感映射。
- Runtime skeleton 线程：Stage 3a / 3b / 4 已落地首轮；下一步围绕 trigger 质量、剧情日志质量和 live demo 验证继续收口，不与 A 的 Phase 1B 混改。
- Content/demo 线程：继续围绕 `边境王国 · 雾潮暮湾` 跑 10-15 分钟真实手测，补 demo case、小说化样例、分镜节点清单和内容 patch 点；同时沉淀 demo persona、字体 shortlist 和 pose brief，给 Phase 2 前置决策做准备。
- 方向落实：旧 `WelcomeView` 任务板、旧 `.advisor-fab`、旧工具化外壳仅做 bugfix，不再继续深度 polish。
- 存储层：按 [docs/plan/local-first-sync-research-20260615.md](./plan/local-first-sync-research-20260615.md) §7 推进 Phase A/B（schema version + IndexedDB 透传 + 批写），不引新依赖；Phase C/D（RxDB / Yjs）只在 multi-device 或协同编辑变成真实需求后再立项。先回到现有 `useStorage.js` 写一稿 schema 迁移注册表设计再决定是否进入 Phase A 实施。
