# Status

<!--
  docs/STATUS.md — multi-session shared state.
  Read at every session start (per AGENTS.md First action step 2).
  Agent may write; user may write. Manual edits only for first version.
  Long-term history does NOT live here; migrate to docs/LOG.md / docs/PLAN.md / docs/src/known-issues.md.
-->

## In flight
<!--
  Per-entry fields (all required unless noted):
    - Owner/session: Codex / Claude / user
    - Worktree: /abs/path (or "n/a" if on main directly)
    - Branch: feature/foo
    - Scope: src/.../file.ts
    - Intent: 正在做什么 (1 句)
    - Last touched: YYYY-MM-DD HH:MM TZ
    - Do not touch: ... (optional, only when there are specific files/areas to avoid)
-->
- Owner/session: user
  Worktree: /home/recoletas/jiuguan/text-game-framework
  Branch: wip/map-realism-render-docs-20260608
  Scope: `src/views/WelcomeView.vue`, `src/layouts/AppShell.vue`, `src/styles/main.css`, `src/pages/{Experience,Writing,Notes,ProseEssay}.vue`, `src/components/{gm-persona/*,workbench/WorkbenchPageHero.vue,QuestLog.vue}`, related UI tests
  Intent: Thread A 正在推进 `kao` / archive-folio UI 重构；按用户 2026-06-10 晚反馈，`WelcomeView` 重新成为 Phase 1B 第一优先：先做 olive/gold kao 档案册首页(3 阶段路径见 [superpowers/specs/2026-06-10-ui-redesign-design.md](./superpowers/specs/2026-06-10-ui-redesign-design.md))，再统一 `AppShell` chrome 与 `Experience` 入口结构，再决定是否把同一套开页语言继续带到 `Writing / Notes / ProseEssay`
  Last touched: 2026-06-10 22:15 CST
  Do not touch: `src/stores/gameStore.js`、`src/services/worldbookContextBuilder.js`、`src/services/generation*`、`src/components/StatusBar.vue`

## Blocked / questions
- 2026-06-09 18:05 CST — `边境王国 · 雾潮暮湾` 内容线程想补“10-15 分钟真实 AI 手测”时，shell 无法复用浏览器 `localStorage` 里的 `apiSettings`；额外尝试公开免配置上游时，`gen.pollinations.ai` 需要有效 key，`text.pollinations.ai` 本轮返回 `429 Queue full`。因此本轮先落成“真实链路排障 + 静态内容审查”，待可用模型配置明确后再补 live 手测记录。

## Recently done
<!--
  Keep the latest 5-10 completed handoffs, newest first.
  Remove entries once they are reflected in docs/LOG.md, docs/PLAN.md, known issues, or commits.
-->
- 2026-06-12 19:00 CST — Claude on `wip/map-realism-render-docs-20260608`: 按用户“左边最大的世界书字体我打算换成艺术字，全面参考调研给我建议”落地 LXGW 霞鹜文楷真艺术字方案。新增 `src/assets/fonts/LXGWWenKai-Regular.woff2` (593 KB, OFL 1.1, GB2312 常用 2700 字 + ASCII + CJK 标点子集) + `OFL.txt`；`src/styles/main.css` 加 `@font-face` for `LXGW WenKai` 与 `--font-display` / `--font-serif` / `--font-sans` / `--font-mono` 四个 token；`index.html` 加 `rel="preload" as="font" type="font/woff2" crossorigin` 让 hero 标题零 FOUT；`src/pages/OpeningPage.vue` 把 `.opening-title-block strong` 从 `Iowan Old Style` 栈切到 `var(--font-display)` 并把 `font-weight: 900` 降到 `400`、letter-spacing 提到 `0.04em`、text-shadow 加一道 `--archive-paper` 高光——LXGW 自身已是书法体，再叠 900 字重反而糊。其余 9 处 `Iowan Old Style` 栈保留走 `var(--font-serif)` 语义（同 main.css 变量）。新增 `uiPolish` 契约 5 条：font-face / 4 token / preload / OpeningPage 用 `var(--font-display)` / 字体文件存在 + < 1 MB。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 39 tests，+5 新增)；`npm run build` 通过 (Vite 自动把 `/src/assets/fonts/...` preload 重写为 hashed `/assets/LXGWWenKai-Regular-DNTqAPtM.woff2` 593 KB，未触发 600 KB chunkSizeWarningLimit 警告)；`npm run test:run` 通过 (87 files, 619 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)。未做 Playwright 视觉验证 (环境无 Chromium/Playwright，Spec R11 safety net 维持 deferred)。字体子集化时遇到 cmap `in` 误判坑 (`cmap.getBestCmap()` 返回 dict-like 含 defaultdict 行为，必须用 `.get(cp) is not None` 严判)，已绕开。霓/晦 等冷僻字未进子集，运行时回退到 `Songti SC` / `STSong` / `Noto Serif CJK SC` 链路，浏览器自动接管。
- 2026-06-12 18:53 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户继续反馈“原本体验页的这部分没删”，完成 `/experience` 残留开场/入口 folio 的二次拆除：`Experience.vue` 顶部 `experience-stage-band` / `playable-world-strip` 整段不再渲染，旧的 `进入世界` / `会话` BookmarkButton、现场摘要、开场钩子、poster/archive strip、`startWorldAdventure()` / `ensureWorldAdventureSession()` / `getLatestSessionForWorldbook()` opening 会话链路均从 `/experience` 移出，并清理 `Experience.vue` scoped CSS 中旧 `experience-stage` / `playable-world` / `stage-command` / `opening-*` selector 残留；`/experience` 现在只保留当前冒险会话工作区、侧栏、速记/顾问/机制等工作态 chrome，`/opening` 继续独占世界选择、开场摘要和 `开局` / `改写` hidden GM 指令。更新 `uiPolish` 契约锁定 `/experience` 不再拥有开场页/入口按钮，opening poster 和 micro stage-command 规则归属 `OpeningPage.vue`。验证：先跑红灯确认旧残留触发 9 个失败；实现后 `npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/welcomeView.test.js src/__tests__/workbenchNav.test.js` 通过 (3 files, 40 tests)；`npm run build` 通过；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`npm run test:run` 通过 (87 files, 614 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)；`git diff --check` 通过；`git diff --check --no-index /dev/null src/pages/OpeningPage.vue` 无 whitespace 输出（exit 1 为 no-index 内容差异预期）。
- 2026-06-12 18:38 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户澄清“脱离 `/experience` 路由”完成开场页路由拆分：新增独立 `/opening` route / `src/pages/OpeningPage.vue`，承接原开场页的 archive-folio 视觉、开场钩子、现场 / 阻力 / 出口摘要、`开局` / `改写` 按钮和 hidden GM 指令发送；`/opening` 自己读取 playable world entry intent、设置 active worldbook、创建/恢复会话并在发送 hidden action 后跳回 `/experience`。`/experience` 删除 `playable-world-opening-page` 和 opening intent 消费逻辑，不再在 mounted / worldbook change 中因开场 intent 自动创建会话；左页 `进入世界` 改为跳 `opening`。欢迎页主入口改 `/opening`，世界选择页 `enterPresetWorld()` 改为保存 intent 后跳 `opening`，工作台体验默认路由和侧栏增加 `开场页`。更新 `welcomeView` / `workbenchNav` / `uiPolish` 契约锁定该分工。验证：先跑红灯确认旧入口/缺新页失败；实现后 `npm run test:run -- src/__tests__/uiPolish.test.js src/__tests__/welcomeView.test.js src/__tests__/workbenchNav.test.js` 通过 (3 files, 40 tests)；`npm run build` 通过；`npm run test:run` 通过 (87 files, 614 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`git diff --check` 通过。
- 2026-06-12 18:15 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户“把这个页面独立出来”继续收 `/experience` 开场页：把原本只剩 header 的 `playable-world-scene-sheet` 改为独立 `playable-world-opening-page` section，保留 `开局` / `改写` hidden GM 指令链路，同时补回页面自身所需的开场钩子、现场 / 阻力 / 出口摘要；新增 `playableWorldOpeningFacts` 专供该独立页使用，不恢复旧的 `opening-scene-grid` / `opening-brief-lines` / `opening-action-directory` / GM 指令预览。更新 `uiPolish` 契约锁定独立页结构。验证：先确认新增契约红灯（缺 `playable-world-opening-page`），实现后 `npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run test:run` 通过 (87 files, 614 tests，含既有地图/axe/canvas stderr warnings 但 exit 0)；`npm run build` 通过；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`git diff --check` 通过。
- 2026-06-12 16:03 CST — Codex on `wip/map-realism-render-docs-20260608`: 修正上一轮对 `/experience` 反馈的误读：不再用 `showExperienceEntryStage` / `currentSession` 隐藏整个顶部体验入口；保留默认体验面板、左侧 `进入世界` / `会话` 和右页 `开局` / `改写`。本轮实际删除的是用户点名的右页冗余现场索引块：`playable-world-route`、`opening-scene-grid`、`opening-action-directory` 及其 `切口目录 / 第一异常 / 01-03` 列表样式残留。`quick-notes-rail`、`GmPersonaLauncher`、`game-image-gen-rail` 继续只在已有用户输入后出现；`MemoryIndicator` 在 `/experience/worldbook` 或 `/experience` 开场过渡态隐藏。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`npm run build` 通过；`git diff --check` 通过。未跑全量 test。
- 2026-06-12 15:31 CST — Codex on `wip/map-realism-render-docs-20260608`: 隔离 `/experience/worldbook` 选择世界过渡页的全局记忆浮层：`App.vue` 改为按 `route.meta.hideGlobalMemory` 条件渲染 `MemoryIndicator`，`experience-worldbook` route meta 增加 `hideGlobalMemory: true`；真正 `/experience` 体验页和其他工作页不受影响，顾问组件本来只在工作页内挂载。更新 `uiPolish` 契约锁定入口页隐藏全局记忆 chrome。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 34 tests)；`npm run build` 通过；`git diff --check` 通过。未跑全量 test/visual。
- 2026-06-12 15:08 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户反馈精简 `/experience` 右页现场卡，删除右侧重复说明块：`opening-brief-lines`（入口 / 阻力 / 出口）和 `playable-world-scene-status`（LIVE / 继续推进）；保留开场页 header、开局 / 改写按钮、现场格和切口目录，不改会话/GM 指令链路。同步删除已无引用的 `playableWorldExitLabel` computed，并更新 `uiPolish` 契约禁止这些说明块回归。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 33 tests)；`npm run build` 通过；`git diff --check` 通过。未跑全量 test/visual。
- 2026-06-12 15:00 CST — Codex on `wip/map-realism-render-docs-20260608`: 修正首页与工作区默认体验入口的路由语义：`WelcomeView` 的 `进入世界` 现在进入 `/experience/worldbook` 世界选择页，`继续` 仍保留 `/experience` 当前冒险入口；`workbenchNav` 的“体验”默认入口同步改为 `experience-worldbook`，体验侧栏拆为 `选择世界` / `当前冒险`，并把 `experience-worldbook` 的 route meta 归到 `activityKey: 'experience'`，避免默认体验页高亮到“设定”。更新 `welcomeView` / `workbenchNav` / `uiPolish` 契约锁定该行为。验证：`npm run test:run -- src/__tests__/welcomeView.test.js src/__tests__/workbenchNav.test.js src/__tests__/uiPolish.test.js` 通过 (3 files, 39 tests)；`npm run build` 通过；`git diff --check` 通过。按用户先前“不要跑那么多验证”的约束，未跑全量 test/visual。
- 2026-06-12 14:48 CST — Codex on `wip/map-realism-render-docs-20260608`: 纠正上一轮对 `/experience` 左页动作的误判：左侧 `进入世界` / `会话` BookmarkButton 已恢复，`进入世界` 继续走 `startWorldAdventure()` / `ensureWorldAdventureSession()` 并受 `gameStore.isLoading || isStarting` 禁用，`会话` 继续作为会话管理入口；保留开场页 header 内 `开局` / `改写` 的小按钮和 hidden GM 指令链路。默认体验现场逻辑修正为：无 current/latest session 但已有 worldbooksIndex 时，`onMounted` 会 `worldStore.ensureActiveWorldbook()` 并 `ensureWorldAdventureSession({ initIfEmpty: false })`，不再清空为未选择世界，也不再强制打开 SessionPicker；无世界书时仍保持空态和导入入口。`uiPolish` 契约更新为锁定左侧按钮存在且接线、默认 fallback 自动准备体验会话、`v-if="hasSelectedWorldbook"` 维持 5 处。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 33 tests)；`npm run build` 通过；`git diff --check` 通过。未跑全量 test/visual。
- 2026-06-12 11:56 CST — Codex on `wip/map-realism-render-docs-20260608`: 【已被 14:48 CST 修正】本轮曾误删 `/experience` 左页 `进入世界` / `会话` BookmarkButton；后续已恢复，当前真实状态以 14:48 CST 条目为准。保留有效部分：`onWorldbookChange()` 选中世界后会直接 `ensureWorldAdventureSession({ initIfEmpty: false })`；`onMounted` 不再使用 `stillMounting` 自检，避免从世界书入口带 intent 进入时提前 return。验证记录保留：当时 `npm run test:run -- src/__tests__/uiPolish.test.js`、`npm run build`、`git diff --check` 均通过。
- 2026-06-12 11:41 CST — Codex on `wip/map-realism-render-docs-20260608`: 继续收敛 `Experience.vue` 开场行动区：`GM 指令` / `selectedOpeningAction.command` 预览保持不渲染，按钮文案压成 `开局` / `改写`；两个按钮移到开场页 header 内并保持左侧紧凑排布，避免 `space-between` 拉出空白；`.stage-command--compact` 和 `.opening-action-actions .stage-command` 统一锁到 88px/28px，移动端不再被 `.stage-command { min-width: 100%; }` 撑满。交互链路改为 `sendOpeningAction()` 先 `ensureWorldAdventureSession({ initIfEmpty: false })`，再 `handleSend(action.command, { hidden: true })`，避免无当前会话时点击像无效，同时不把内部 GM 指令写进对话 UI。更新 `uiPolish` 契约锁定短文案、隐藏指令、88px compact 按钮和会话先建后发。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 29 tests)；`npm run build` 通过；`git diff --check` 通过。按用户“不要跑那么多验证”的约束，本轮未跑全量 test/visual。
- 2026-06-12 10:55 CST — Codex on `wip/map-realism-render-docs-20260608`: 修复 `WelcomeView` 海报入口分流。此前 `进入世界` 与 `新卷` 都指向 `/experience/worldbook`，导致主入口和创建入口语义重复；现在 `进入世界` 指向 `/experience`，`继续` 仍指向 `/experience`，`新卷` 保留 `/experience/worldbook`。新增 `welcomeView.test.js` 契约锁定三 CTA 分工。验证：`npm run test:run -- src/__tests__/welcomeView.test.js` 通过 (1 file, 4 tests)；`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 28 tests)；`npm run build` 通过；`git diff --check` 通过。
- 2026-06-12 10:48 CST — Codex on `wip/map-realism-render-docs-20260608`: 修复 `/experience` Pass 4 残留交互问题。`Experience.vue` mount 时若 `currentSessionId` 未恢复但本地 `sessions` 存在，会按 `updatedAt` 自动载入最近会话并保持主体验界面可见；完全无会话时也不再强制打开 `SessionPicker`。同步把多段历史 `.stage-command` / `.stage-command--compact` / `.opening-action-actions .stage-command` 大尺寸残留收敛到 144px micro action tab，避免按钮继续撑成大块；`WelcomeView` 默认 82px 按钮未改。新增/更新 `uiPolish` 契约覆盖最新会话恢复、no forced picker、micro 宽度和 opening action flex。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过 (1 file, 28 tests)；`npm run build` 通过；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过 (1 file, 12 tests)；`npm run test:run` 通过 (87 files, 607 tests，含既有地图/axe stderr warnings 但 exit 0)；`git diff --check` 通过。
- 2026-06-12 10:30 CST — Claude on `wip/map-realism-render-docs-20260608`: 完成 Pass 3 收口 + Pass 4 (1-click resume + 按钮密度) 落地。两条独立 commit (1 commit per feature)：`7d7b4bd chore(agents): update skill discovery paths for Codex/Claude compat` (2 files)、`e1f6a03 feat(ui): ui-redesign pass 2-4 completion — 1-click resume + micro button density` (51 files, 9620 +/- 1277)。Pass 4 实质改动：① `gameStore.getLatestSessionForWorldbook(worldbookId)` Pinia action (按 `updatedAt` 倒序，按 `worldbookId || worldId` 找)，`Experience.startWorldAdventure` 直接调它做 resume vs create 分支；② `isStarting` ref + try/finally 防双击；③ `BookmarkButton` 新增 `size: 'default' | 'compact' | 'micro'` prop (含 validator + `--size-micro` class + 760px 移动端 36px 变体)，`Experience` 4 个 stage-command 全部改用 `size="micro"`；④ 6 处 `.stage-command*` min-height 收敛 (62→40 / 50→36 / 58→40 / 56→40 / 78→40 / 72→36)；⑤ Pass 3 contract 测试的 kao grammar regex 修复：用 `g` flag + `find()` 按 `isolation: isolate` 过滤，去掉与 `playable-world-strip.is-empty .playable-world-stage-poster` 复合选择器的歧义；⑥ 顺手清掉 Pass 2 留下的 `welcome-collage-tile--9` 三处残留 (template span / CSS rule / shared selector)。`WelcomeView` 3 个 `BookmarkButton` 保持 82px default 不动。Spec: `docs/superpowers/specs/2026-06-11-welcome-experience-pass4-resume-and-density-design.md` (v3)；Plans: `docs/superpowers/plans/2026-06-11-welcome-experience-pass{2,3,4}.md`。验证：`npm run test:run` 通过 (87 files, 605 tests，含 9 个新增 Pass 4 contract + 1 个 Pass 3 regex 修复)；`npm run build` 通过；`npm run docs:build` 通过；`git diff --check` 通过；2 个 demo 二进制 (`docs/demo/first.png` / `docs/demo/kao.jpg`) 故意未 commit。Playwright 视觉验证 (Spec R11) 仍因环境无 Chromium/Playwright 未执行；该 safety net 维持 deferred。
- 2026-06-12 09:39 CST — Codex on `wip/map-realism-render-docs-20260608`: 接入 Codex 侧 Superpowers skills。确认 Claude 侧 `.claude/settings.json` 已启用 `superpowers@claude-plugins-official`；本轮把本机已缓存的 14 个 Superpowers skill 从 `/home/recoletas/.codex/.tmp/plugins/plugins/superpowers/skills/` 安装到 Codex 全局 `~/.codex/skills/`，包括 `using-superpowers`、`brainstorming`、`writing-plans`、`subagent-driven-development`、`systematic-debugging`、`verification-before-completion` 等。同步更新 `AGENTS.md` First action，兼容 Claude 的 `superpowers:using-superpowers` 与 Codex 的 `using-superpowers` 命名，并删除 `CLAUDE.md` 空占位注释。验证：14 个全局 Superpowers `SKILL.md` frontmatter 可读；`.agents/skills/*/SKILL.md` 与 `.claude/skills/*/SKILL.md` 仍全部解析到仓库内 `agent-skills/*/SKILL.md`；`git diff --check` 通过。注意：当前已启动的 Codex 会话不会热加载新 skills，需要重启 Codex 后才会在可用 skill 列表中出现。
- 2026-06-11 14:52 CST — Claude on wip/map-realism-render-docs-20260608: Pass 2 收尾, 4 个 Issue 全部修完(Welcome 主图软过渡 / 01 按钮可见 / 7-tile A3 中密度 / Experience 综合修)。验证: `npm run test:run` (全绿)、`npm run build` (通过)、`npm run docs:build` (通过)、`git diff --check` (clean)。截图见 `docs/demo/pass2-screenshots/`。Spec: `docs/superpowers/specs/2026-06-11-welcome-experience-pass2-design.md` (v3, `7f98157`)。
- 2026-06-10 22:15 CST — Codex on `wip/map-realism-render-docs-20260608`: 推进 Thread A / Phase A-C 的可运行切片：补强 `src/components/folio/{BookmarkButton,PosterStage}.vue`，让 BookmarkButton 能把旧页面内部 class 透传到 index/label，且 PosterStage 不再默认把交互入口整体 `aria-hidden`；`WelcomeView` 保留既有源码契约并挂回 `welcome-command-*` 样式；`AppShell` 抽屉改用 `FolioSurface` chrome 外壳；`Experience` 的世界入口 / 开场入口按钮改用 `BookmarkButton`，并加轻量 `ArchiveStrip` 作为现场档案缩略条。未触碰 `gameStore`、`worldbookContextBuilder`、`generation*`、`StatusBar`。验证：`npm run test:run -- src/__tests__/welcomeView.test.js src/__tests__/uiPolish.test.js` 通过（2 files, 9 tests）；`npm run build` 通过；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过（1 file, 12 tests）；`npm run test:run` 通过（87 files, 585 tests，含既有地图/a11y stderr 警告但 exit 0）；`git diff --check` 通过。Dev server 启在 `http://127.0.0.1:5177/`，`/welcome` 与 `/experience` 均返回 200；当前环境无 Chromium/Playwright，未完成真实截图审查。
- 2026-06-10 20:01 CST — Codex on `wip/map-realism-render-docs-20260608`: 继续按用户“去掉没用的框、体验页至少像欢迎页一样立体”的反馈收 `src/pages/Experience.vue`。这轮把入口从“三列碎片”压成真正的双页 folio：左页只留世界标题 / 开场钩子 / 主动作，右页只留现场海报 / 开场线索 / 切口目录；删掉独立左侧切口栏和右栏解释句，把切口改成页内书签目录，标题切回 serif display，CTA 和现场卡统一到更接近 `WelcomeView` 的书签 / 相片 / 纸页语法。同步更新 `src/__tests__/uiPolish.test.js`，锁住“不再回到 `experience-entry-rail` 和说明型 filler 文案”的契约。验证：`npm run test:run` 通过（87 files, 584 tests；含既有 stderr warnings 但 exit 0）；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过（1 file, 12 tests）；`npm run build` 通过。
- 2026-06-10 19:25 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户反馈清掉多个界面里的 `Playable Worldbook` 空抬头：`WelcomeView` 去掉 appmark 副标题和海报 kicker，`Experience` 空态不再显示该英文标签，`WorldBookQuickImport` 去掉 hero kicker；同步更新 `uiPolish` / `welcomeView` 契约。验证：`npm run test:run` 通过（87 files, 584 tests；含既有 stderr warnings 但 exit 0）；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过（1 file, 12 tests）；`npm run build` 通过。
- 2026-06-10 19:20 CST — Codex on `wip/map-realism-render-docs-20260608`: 按用户反馈收掉 `AppShell` 常驻顶栏，删除 `Pinax / 路由标题 / 01-05 tab / Mode xx` 横向 mast，只保留左上悬浮导航触发器和原有抽屉；同步更新 `uiPolish` 契约。验证：`npm run test:run` 通过（87 files, 584 tests；含既有 stderr warnings 但 exit 0）；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过（1 file, 12 tests）；`npm run build` 通过。
- 2026-06-10 18:55 CST — Codex on `wip/map-realism-render-docs-20260608`: 根据用户“主页暂时不动，体验页完全不行”的反馈，只重做 `src/pages/Experience.vue`，不再推进首页；把原来碎成 `入口摘要 + 开场卡 + 右侧切口栏` 的三段式结构压成单一开页：左页保留世界标题 / 当前任务 / 两个主动作，右页只保留一个现场海报与当前开场信息，切口目录改成页底目录条，整体减少分裂块和重复说明。未碰 `WelcomeView`、`gameStore`、`worldbookContextBuilder`、`StatusBar`。验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 通过（1 file, 7 tests）；`npm run build` 通过；`git diff --check` 通过；`npm run test:run` 通过（87 files, 584 tests，含既有 stderr warnings 但 exit 0）；`npm run test:run -- src/__tests__/visual-verification.test.js` 通过（1 file, 12 tests）。
- 2026-06-10 18:32 CST — Codex on `wip/map-realism-render-docs-20260608`: 新增 [docs/plan/kao-ui-direction.md](./plan/kao-ui-direction.md)，并把它接到 `docs/PLAN.md` / `docs/plan/README.md`，明确 `kao.jpg` 作为首页与 chrome 的视觉执行锚；同时给 `src/styles/main.css` 增加首轮 archive / folio palette tokens，并把 `src/views/WelcomeView.vue` 改成基于 `docs/demo/kao.jpg` 的档案册首页首刀：纸页底、相片拼贴、暖金/氧化绿配色、极少文案、书签式 CTA。同步更新 `src/__tests__/uiPolish.test.js`，让契约反映 Experience 已切离 `WorkbenchPageHero` 的现状。验证：`npm run test:run -- src/__tests__/welcomeView.test.js src/__tests__/uiPolish.test.js` 通过（2 files, 8 tests）；`npm run build` 通过。
- 2026-06-10 11:15 CST — Codex on `wip/map-realism-render-docs-20260608`: 撤回 `WelcomeView` 里错误的 CSS 假人物占位，改回诚实的海报舞台：保留海报框、光区、反射层和 CTA，不再用假剪影冒充角色海报；同步更新 `welcomeView` / `uiPolish` 契约测试，并把 `docs/PLAN.md` / `docs/plan/playable-worldbook-parallel-plan.md` 的 Phase 1B 约束改成“真实位图资产接入前，不再使用 CSS 假人物 / 假剪影”。验证：`npm run test:run -- src/__tests__/welcomeView.test.js src/__tests__/uiPolish.test.js` 通过（2 files, 8 tests）；`git diff --check` 通过。

## Next up
<!--
  Things planned but not yet started. Move into "In flight" when picked up.
-->
- UI shell 线程：按 `character-driven` §3.4 / §3.5 + [superpowers/specs/2026-06-10-ui-redesign-design.md](./superpowers/specs/2026-06-10-ui-redesign-design.md) 继续 Phase 1B，先稳住 `WelcomeView` 的 olive/gold kao 档案册首页(3 阶段路径：CSS tokens + 自包含 utility → 4 kao 组件 + 模板局部重排 → 3 文件复用 + 1 次人工截图)，再统一 `AppShell` 的 hidden-first chrome；在真实位图资产接入前，不再使用 CSS 假人物 / 假剪影；不提前滑进 Motion One / GSAP。
- UI shell 线程：在首页和 chrome 稳定后，再推进 Phase 1C，把 `Experience / Writing / Notes / ProseEssay` 分别对齐到正立对话态、侧视静默态和编辑态的在场感映射。
- Runtime skeleton 线程：Stage 3a / 3b / 4 已落地首轮；下一步围绕 trigger 质量、剧情日志质量和 live demo 验证继续收口，不与 A 的 Phase 1B 混改。
- Content/demo 线程：继续围绕 `边境王国 · 雾潮暮湾` 跑 10-15 分钟真实手测，补 demo case、小说化样例、分镜节点清单和内容 patch 点；同时沉淀 demo persona、字体 shortlist 和 pose brief，给 Phase 2 前置决策做准备。
- 方向落实：旧 `WelcomeView` 任务板、旧 `.advisor-fab`、旧工具化外壳仅做 bugfix，不再继续深度 polish。
