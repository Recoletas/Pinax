# 01 - 会话稳定

状态：完成

## 已完成

- 会话运行态和全局写作资料已拆分。
- `createSession()` 支持明确参数，默认新建空运行态。
- 会话保存/恢复覆盖消息、历史、世界书、写作角色、时间、地图、活动等运行态。
- 新建、切换、删除、重写后续等关键路径已补保存点。
- 世界书切换增加保护，不再静默覆盖当前剧情。
- 启动默认世界书和新建会话初始化相关 bug 已多轮修复。

## 验收状态

| 验收项 | 状态 |
|--------|------|
| 新建会话不清全局资料 | 完成 |
| 刷新后恢复当前会话 | 完成 |
| 删除消息不回魂 | 完成 |
| 重写后续可持久化 | 完成 |
| 世界书切换不静默覆盖剧情 | 完成 |

## 后续只做

- 会话初始化/缓存相关 bug 复现时，再查 prompt 传参和运行态边界。
- 不继续扩写第二套体验 store，短期以 `gameStore` 为主。
