# UI Polish 收口记录

状态：P2-A / P2-B / P2-C 已实施

> 本文件只记录当前 UI polish 的收口结果，不作为新的功能计划入口。页面路由过渡已由 [page-transition.md](./page-transition.md) 收口完成，本文件不再重复展开。

## 1. 执行原则

- 只修现有视觉层次、反馈节奏和可读性问题。
- 不新增页面、入口、状态 store、动画库或主题系统。
- 不改变路由、业务数据流、生成链路和持久化逻辑。
- 当前并行中的世界书显示问题和智能顾问优化不纳入本文件执行范围。
- 若某个待办会碰到世界书或顾问相关文件，应拆到对应专题处理，不在本轮 UI polish 中抢改。

## 2. 已完成项

- P2-A 画布背景层次：`ProseEssay` 卡片画布增加低对比点阵工作区底纹，侧栏和节点布局不变。
- P2-B 已有弹窗过渡统一：体验、写作、素材和画布现有弹窗接入 `modal-fade` / `modal-scale`，并加入 reduced motion 分支。
- P2-C 暗色模式 accent 按钮可读性：新增 `--accent-text`，将相关 accent 背景按钮文字统一到该 token。

验证：`npm run test:run -- src/__tests__/uiPolish.test.js` 与 `npm run build` 均通过。

## 3. 暂缓与避让

- 世界书显示问题：由并行世界书工作处理，本文件不列具体修复步骤。
- 智能顾问优化：由并行顾问工作处理，本文件不列 prompt、接口、多轮上下文或主动顾问方案。
- 页面路由过渡：已完成并归档到 `page-transition.md`。
- 复杂主题系统：暂不做 token 重构、调色板重建或全局组件库迁移。
- 新视觉功能：不新增仪表盘、引导页、帮助浮层或装饰性背景。

## 4. 验证清单

后续实施任一项 UI polish 时，至少检查：

1. 视口：360px、768px、1280px。
2. 主题：亮色和暗色。
3. 动效：正常动效和 `prefers-reduced-motion: reduce`。
4. 核心路径：体验到素材、素材到写作、素材到画布、分镜保存。
5. 构建：`npm run build`。

文档维护要求：

- 每次只把已确认要做的 polish 项放进本文件。
- 已完成项保留一行结果摘要即可，不继续展开过程。
- 若某项开始涉及业务逻辑或新功能，应移出本文件，另行进入对应专题计划。
