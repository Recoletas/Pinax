# 02 - 世界书上下文

状态：完成

## 已完成

- 已抽出 `src/services/worldbookContextBuilder.js`。
- 世界书上下文构建支持 runtimeState、关键词扫描、常驻/高优先级条目和 token budget。
- 体验生成、Copilot 等链路已使用统一世界书上下文。
- 提示词详情/世界书预览链路已接入，可查看命中条目和上下文。
- 世界书草稿不自动写回正式世界书，改为素材收件箱显式确认入库。

## 验收状态

| 验收项 | 状态 |
|--------|------|
| 世界书注入顺序稳定 | 完成 |
| 常驻/高优先级条目优先 | 完成 |
| 用户能看到命中内容 | 完成 |
| 新设定不直接污染世界书 | 完成 |

## 后续只做

- 优化预算报告展示。
- 补更多世界书条目类型的 UI 解释，不改变核心构建逻辑。
