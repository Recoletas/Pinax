# ABC 集成后的续作任务书

> 9 月 17 日晚调度更新：本文保留未完成任务作 backlog；今晚按[资料/UI/记忆新三线](./nightly-20260917-sources-ui-memory.md)及其写锁执行。下文“UI 保留给 Codex”是白天安排，已被新分工替代；旧 A 记忆由 N-C 承接，旧 B 漫画/旧 C 跑团不自动续跑。本文不作为今晚独立启动指令。

状态：计划，未执行。2026-09-17。由用户另行交给 agent；本会话不启动 worker。

## 开工与边界

以 `main` 的 `5e14e39`（ABC 组合审查修复）为最低基线，从主线新建工作树。先读 AGENTS、STATUS、LOCAL（非空时）及触发的 skill，再读 `docs/agent-runs/nightly-20260917/integration-summary.md`。禁止直接续用旧夜间树并整树覆盖，不重复应用已集成的 patches。不要把原 AX48/CX48/BX12 标为整线完成。

用户可以让一个 agent 按 A→C→B 顺序执行，也可以分三个隔离工作树并行。不存在 O 线：各线自己负责迁移、测试、安全、许可、文档和交付。白天 Codex 统一审查合并。

**本轮 UI 保留给 Codex**：AppShell、WorkspaceTabs、styles、Authoring.vue / Authoring.block-native.css、authoring 组件、DocsPage、WorldMapPage、geography 组件、StructuredSettings 与 worldbook 编辑 UI。ABC 不修改这些文件，不修改全局 STATUS/PLAN/LOG，不做全局格式化。需要接线时提供带前置条件的接口说明和最小建议 diff，由白天合并；不得用未接入的 helper 宣称生产完成。各线只能更新自己的交付回执。

建议所有权：A `services/memory*` 及其现有归域目录、数据库/备份相关模块；B 漫画仓储、媒体恢复服务及漫画专属组件；C experience 跑团服务和专属组件。共享 `gameStore` / reader / backup / coordinator 按以下依赖串行，不由两个 worker 同写。先列实际文件清单并标出重叠再开工，不臆造目录。新 UI 必须复用已有原语，不自建第二套设计系统。

## 共同完成定义

每个任务交付：实现位置、生产调用者、成功与失败断言、回归命令/exit code、剩余边界。每完成 3 个任务写一次回执；不得只写设计、堆 mock、打印“通过”或用任务数量代替闭环。未满足接线门禁明确写“域实现完成，生产未接入”。

核心 Vitest 总预算保持 20 文件 / 200 用例，补断言进现有用例，额外场景放显式 scripts。最终 `npm run verify:full` 必须 exit 0；各域额外 smoke 不可省略。浏览器使用隔离 profile / 专用端口，禁止操作用户 5173 数据。禁止付费调用、真实用户迁移、部署、推送或删旧树。真实模型质量保留独立未验标签，模拟质量不替代它。复用开源固定 commit、核许可证和实际源段，随包保留许可；Utopia 是语义参考，不意味着可复制不兼容的存储架构。

## A：历史、事实与角色知情（承接 AX18 及后续知识/冲突任务）

已有 Dexie v2、双时间分页、生产书级读取和 ZIP，不重做这些基础。

| ID | 实现与交付 | 必须包含的反例/验收 |
| --- | --- | --- |
| A01 | 对照 Utopia 固定源码，列事实、叙事时间、记录时间、知情者、传播、修正的映射；注明直接复用/借鉴/不适用 | 每项指向真实文件与许可证；不把作者全知当角色知情 |
| A02 | 定义带版本的角色知识读取合同，参数包含 book/worldbook/branch/character/asOf、预算和取消信号 | 缺角色、缺域、未授权、旧存档全部 fail closed；禁止 active worldbook 兜底 |
| A03 | 以不可变知情事件记录亲历/被告知/推断，链接事实版本与来源；沿用数据库 owner | 不接受裸字符串角色名越权；未知证据、跨书引用拒绝；事务失败零半写 |
| A04 | 实现按角色、分支与双时间过滤的批量读取及稳定游标 | 未来事实、私密未传播事实、兄弟分支、撤销证据不泄漏；同时间排序稳定 |
| A05 | 实现显式传播事务：谁在何时向谁透露什么，保留链路而非复制全知集合 | A→B 不自动让 C 知情；重复传播幂等；链断裂可解释 |
| A06 | 来源修改/删除只追加失效或待复核事件，保留旧版本可追溯 | 历史时点仍可读原证据，当前召回排除失效；撤销不复活无权事实 |
| A07 | 冲突候选分组、版本比较、人工决定的仓储接口；不按最后写入自动裁真 | 互相矛盾、同内容不同来源、并发审阅、一方已失效均有明确结果 |
| A08 | 长历史查询 view-model：分页、筛选、证据展开、定位及取消；向 Codex 提供历史页接线合同 | 10k+事实混合样本不全库读进 UI；换书迟到返回丢弃；空/错误/重试有状态 |
| A09 | 生产 reader 接入角色合同，仅在请求明确要求角色视角时使用；缓存键带 revision/权限/时间 | 同书不同角色不共享缓存；摘要不得绕过权限；token 预算内优先级可解释 |
| A10 | 新表/事件纳入 ZIP 与迁移版本；旧包缺域不清除新知识；保留补偿机制 | 伪造权限/断引用/序号冲突拒绝；导出冻结一致；故障后媒体、来源、账本均恢复 |
| A11 | 接 C 的公开/私密场景测试语料，交付角色读取契约 smoke | 先完成 A02–A05 才解除 C 的 public-only 降级；不能靠提示词防泄漏 |
| A12 | 交付迁移演练、性能与用户恢复说明，以及 SQLite/跨设备可行性 ADR | 合成数据与真实用户迁移分开标注；SQLite/云同步本轮只决策，不擅自引入第二真源 |

A 回执：`docs/agent-runs/continuation-20260917/a-summary.md`，逐任务标完成/部分/未开始。必跑现有 memory 三套 smoke、ABC ZIP/reader 组合脚本和新增权限反例。A10 写 backup 时 B 不修改该 owner。

## B：漫画可靠制作闭环（承接持久恢复与 BX 公共工程）

已有按书页目录、阶段请求围栏、仅重试保存。不要重写整个漫画页；与 Codex UI 改动分离。

| ID | 实现与交付 | 必须包含的反例/验收 |
| --- | --- | --- |
| B01 | 盘点 StoryForge 固定源的阶段生成、角色参考、分镜编辑与重试路径，输出真实差距 | 分清参考设计与直接代码移植；许可随包、不引进整套后端 |
| B02 | 将内存最多三项失败结果改成有上限的持久恢复队列：书/页/阶段/requestId/源 revision/媒体 | 刷新保留可保存结果；持久写失败明确提示仍需下载，不虚报已备份 |
| B03 | 恢复记录状态机：待保存/保存中/已保存/源已变化/需人工处理，显式只重试保存 | 重试零 provider 请求；重复重试不多建媒体；别书永不出现 |
| B04 | 已返回媒体落库与队列确认一致；崩溃中间态启动时可协调 | 在媒体成功而确认失败处注入故障，刷新可对账，无孤儿和重复页 |
| B05 | 未知请求与有结果未保存分开管理；展示原请求身份与成本风险 | timeout/5xx/断网保持未知；仅用户明确确认重发后允许新请求；不承诺 exactly-once |
| B06 | 队列容量、过期与清理策略：预览影响、导出后删除、按书隔离 | 达上限拒绝新任务或明确要求处理，不能静默丢最旧结果；删除作品需显式说明 |
| B07 | 有界批量执行：预览选中页、并发上限、逐页状态、取消未开始任务、部分完成可恢复 | 取消不撤销已完成；切书不污染；断网不自动重发未知计费请求 |
| B08 | 角色参考冻结：稳定角色 ID、参考图 hash/版本、页级 override，失效提醒 | 改人物资料不偷偷改已生成画面；旧引用缺失阻止声称一致；不使用跨书头像 |
| B09 | 分镜到图片的溯源与 stale 判断：编辑源文本/对白/顺序后只失效关联阶段 | 改一格不全书重做；删除格的迟到结果不能重新插回；撤销后身份不混淆 |
| B10 | 导出检查真实可读资源、顺序、部分失败与缺图，队列数据接现有备份扩展接口 | A10 完成后才共享 backup 接线；缺图不导出“成功”空白；恶意链接不被自动外发 |
| B11 | 整理单一页选择 owner 与重复导航，漫画专属局部精修遵守 Codex 共享 tokens | 不改共享壳；1440/900/390、长页名、键盘、暗色、恢复/未知状态截图并实际查看 |
| B12 | 增强 comics-nightly-check：真实 IndexedDB 刷新、持久故障、重复点击与批量中断 | 非 mocked 仓储成功；实际计数校验零额外模型请求；真实图片质量另列待用户评测 |

B 回执：`docs/agent-runs/continuation-20260917/b-summary.md`。公共备份只提交适配模块与建议接线，A10 合入后串行接入。不要扩大媒体配额而不设计满盘路径。

## C：跑团主持与连续战役（承接 CX 后续，不包含 C2 联机）

已有 2d6、durable pending、outbox、原创小场景、资源快照及公开信息旅伴建议；并非自主 AI KP。

| ID | 实现与交付 | 必须包含的反例/验收 |
| --- | --- | --- |
| C01 | 固定 StoryForge 源，映射主持循环/角色/检定/场景/记忆；定义本期轻规则边界 | 不混入其不可分发资源；不承诺 DND/COC 完整规则或多人联机 |
| C02 | 主持请求冻结行动、当轮世界快照、允许 NPC、规则结果、玩家控制边界 | 玩家取消/切书/切分支后迟到结果不提交；不能替玩家说话和选择 |
| C03 | 分开“拟议后果”和“规则结算”，结构化提案经验证后由 coordinator 一次提交 | 模型不能改骰、改已有 receipt、凭空扣资源；解析失败无部分结算 |
| C04 | 模型旅伴接 A02 合同，角色私密输入逐个隔离；A 未就绪维持明确公开降级 | 含秘密的对抗语料零越权；模型建议不等于已执行行动 |
| C05 | 多轮主持预算：每次玩家行动最多有限内部步骤、token/时间上限、可取消 | 禁止无限自动续跑与未经授权计费；工具重复返回触发停止而非死循环 |
| C06 | 叙述证据检查与可解释拒绝：事件/人物/资源必须属于冻结集合 | 幻造 NPC、未来知识、改写失败为成功、命令玩家行为均不自动采纳 |
| C07 | 战役保存模型与会话切换：场景进度、角色、资源、线索、引用统一 owner | 新开战役不继承旧资源；刷新继续不重复开场；配额失败保留原状态 |
| C08 | 祖先分支资源重建：基于共同祖先快照和合法事件回放，不扫兄弟分支 | 两处分叉、回溯后再次分叉、重叙述、重试均不重复扣资源 |
| C09 | 旧快照缺 runtime 的显式兼容政策与用户提示，不虚构缺失历史 | 无法重建标记受限，不能把当前资源当历史真值；旧档保持可打开/导出 |
| C10 | 规则扩展注册合同：骰型/修正/结果/资源效应，保持现有轻规则默认 | 不可信表达式不 eval；未知规则拒绝；保存规则版本防更新后重新解释旧骰 |
| C11 | outbox 长积压、容量背压与恢复：回执只作近期索引、历史查数据库 | 超过 1000 不裁丢；库暂不可用不阻塞已保存叙事；归档幂等且仅在 durable 后 |
| C12 | 跑团专属可访问性和整套评测：修 nested-interactive、对比度，补真实 coordinator 反例 | 保留 196/46 既有门禁；新增公共/私密/代答/矛盾语料，模型 mock 与真实质量分开报告 |

C 回执：`docs/agent-runs/continuation-20260917/c-summary.md`。C04 等 A 的契约版本与反例通过后再接，先做 C02/03/07–11。不合并另案 C2 分支，不改 Authoring 或共享右栏。

## 执行顺序与停止条件

第一批：A01–05；B01–05；C01–03、07。第二批：A06–09；B06–09；C05–06、08–10。第三批：A10–12 → C04/11/12、B10–12。A/C 角色合同先冻结再串行接线；不能通过各自发明同名接口“并行完成”。

触及用户 UI 保留区、需要真实密钥/付费、破坏迁移、根许可证变化、需要服务器/SQLite 新真源时，停止该任务并提交具体决策项，继续不依赖它的任务。不能为了保持忙碌擅自扩范围。

交付给白天的内容：基线和 tip SHA、干净工作树、逐项结果、依赖版本、测试日志路径与退出码、截图、许可变化、迁移/回退步骤、需主线接入的精确位置。白天重新验证组合故障路径后才决定合并。用户体验和真实模型验收始终单列。
