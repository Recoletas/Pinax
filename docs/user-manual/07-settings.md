# 设置详解

右上角的 **设置** 打开设置面板，三个标签：**外观**、**AI 配置**、**存储**。

## 外观

- **主题**：两套主题（主题一 / 主题二），各带亮 / 暗模式。明暗切换也可以在欢迎页顶部的按钮快速切换
- **UI 缩放**：100% / 95% / 90% / 85% 四档。觉得界面偏大就调小（默认 85%）

## AI 配置

文本模型用**「配置列表 + 新增」**模式管理：一条内置 MiniMax + 你自己的配置，跟图片 / 视频模型同一套交互。

### 内置 MiniMax（默认，开箱即用）

- 打开 **AI 配置** 就有一条 **MiniMax（内置）** 配置，**默认选中**，不额外配置即可用
- **不可编辑、不可删除**；它的 API Key 由**服务器**持有（`server/.env` 里的 `MINIMAX_API_KEY`），客户端只显示「已由服务器配置」，你的浏览器永不接触真实 Key
- 想换用你自己的配置，点「添加模型配置」，或在列表里选内置项查看详情

### 自定义文本模型配置

- 点 **「添加模型配置」** 新增；已加配置可**任意编辑 / 删除**，和图片 / 视频模型一致
- 配置项：**名称**、**渠道**（Provider）、**API 地址**、**API Key**、**模型**，外加 **测试连通性**
- 切渠道自动带出默认 API 地址和默认模型（MiniMax 为 `MiniMax-Text-01`，DeepSeek 为 `deepseek-v4-flash`）
- 渠道选 **MiniMax** 时可以不填 Key——未填会用服务器内置密钥
- **测试连通性**：验证 Key 和网络通不通，还会探测协议 / 推理能力 / 延迟

可选渠道一览：

| 渠道 | 适合 |
| --- | --- |
| MiniMax (Anthropic 兼容) | 内置默认，开箱即用 |
| DeepSeek | 性价比高，中文友好 |
| OpenAI | 通用 |
| SiliconFlow | 国内，便宜 |
| OpenRouter | 一个 Key 试多家模型 |
| Ollama (本地) | 完全离线、零费用（需本机跑 Ollama） |
| Groq | 速度极快 |
| Moonshot | 中文长文 |
| 自定义 | 其他兼容 OpenAI 协议的 |

> **新手建议**：什么都不用配，直接用内置 MiniMax 就好。想用别的模型时再加一条配置。

### 图片 / 视频模型

图片和视频的模型配置在画布的生图侧栏 / 生成视频面板里管理：

- **图片**：默认已有一条 **MiniMax Image**（`image-01`）配置，填 Key 就能用；也支持 OpenAI DALL·E、Stability、SD WebUI、ComfyUI 或自定义 HTTP
- **视频**：默认 **MiniMax Hailuo**（`MiniMax-Hailuo-2.3`），分辨率 768P；也支持自定义异步 HTTP

模型配置存一次就记住，不用每次重新填 Key。

## 存储

你的所有内容——世界书、素材、写作、画布、会话——都存在**这个浏览器**的本地存储里。存储标签让你：

- 看**存储用量**：百分比条 + 用量状态（充足 / 建议导出 / 请立即导出备份）
- 看 **Top 10 占用**：哪些数据占的空间最大
- **导出全部备份**：把全部数据存成一个 JSON 文件
- **导入备份**：把之前导出的 JSON 导入（会先算出新增 / 覆盖 / 跳过计划，确认后写入）

> **强烈建议定期导出备份**，尤其是你写了很多内容之后。本地存储不会自动上云，浏览器数据被清 = 数据没了。

## 记忆服务（可选）

冒险中采纳的记忆默认**保存在本地**。如果你想跨浏览器 / 跨设备同步这些偏好，可以配置记忆服务（在浏览器本地存 Key）。没配也不影响使用——采纳的记忆仍会在本会话生效。
