# 设置详解

从右上角或工作台左下角打开 **设置**。当前设置分为写作、外观、记忆与历史、AI 配置、体验和备份与恢复。

## 写作

这里的排版偏好与正文工具栏共用并立即生效：

- 字体、字号、行距和段落间距。
- 首行缩进。
- 文本块边界：当前块、全部边界或隐藏。
- 打字机滚动和段落聚焦。

这些设置只改变写作与阅读方式，不改正文内容。正文自动保存始终开启；专注模式从正文工具栏切换。

## 外观

可以在 **日间**和**夜间**配色之间切换，也可以调整界面缩放。夜间模式还可从右上角快速切换。外观和缩放只影响显示，不修改作品。

## 记忆与历史

Pinax 的记忆由应用内的本地数据库管理，无需另行连接“记忆服务”才能使用。在 **记忆与历史** 中可以查看记忆内容、事实及其来源、决定和修订记录，也能检查数据库健康情况。正文旧稿则在工作台的 **历史** 中查看。

当前实现参考 Utopia 的事实、证据与时间模型，并移植了部分时序算法；数据库运行在浏览器的 Dexie/IndexedDB 中。它保留故事中何时发生、系统何时记录等信息，为相关创作功能提供可追溯的参考，并非把 Utopia 整套服务直接接入 Pinax。

候选建议与正式事实分开保存，修改也有相应记录；数据库并不意味着 AI 生成的所有内容都会自动成为正式事实。部分旧候选仍使用兼容存储，不能理解为所有作品数据都已经迁入同一个数据库。完整工作区 ZIP 可用于备份记忆历史与事实账本，当前没有自动云同步。

旧数据如果没有原始修订记录，只能保留迁移时快照，不能凭空重建历史。

## AI 配置

**写作本身不需要配置 AI。** 只有使用问答、改写、推演、生图或视频等相关功能时才需要可用的模型渠道。

### 站点提供的内置渠道

界面会显示不可编辑、不可删除的内置 MiniMax 配置，但它只有在部署者已经配置服务端密钥并且网络可用时才能调用。普通用户可以选中该配置并点 **测试连通性**：

- 测试成功后再使用相关功能。
- 出现“服务器未配置 `MINIMAX_API_KEY`”时，记录错误并联系站点部署者；普通用户不需要修改服务器文件。

浏览器不会取得内置渠道的真实密钥。

### 添加自己的文本模型

点 **添加模型配置**，按界面填写名称、渠道、API 地址、API Key 和模型，然后保存并测试连接。可选渠道和默认模型可能随版本变化，以当前界面和渠道官方说明为准。

自定义配置保存在当前浏览器。换网址、浏览器或设备时不会自动带过去，且不会进入作品备份；请用安全方式另行保管密钥，不要发进反馈、截图或公开仓库。

图片和视频模型在对应生成工作台中管理。是否支持参考图、异步任务或本地服务取决于所选渠道；不要把界面中存在的内置项理解为所有部署都已开通。

## 体验

这里调整体验模式的单次续写篇幅和阅读密度。它影响之后的生成长度或当前显示，不会改写已经显示的正文。

## 备份与恢复

这里提供两种备份，用于保留副本或换设备；导出本身不会释放空间：

| 备份 | 包含 | 不包含或不保证包含 |
| --- | --- | --- |
| 完整工作区 ZIP | 书稿、设定、来源归档、已落盘媒体、记忆历史和事实账本 | 模型密钥；外部链接引用、尚未落盘媒体和浏览器缓存不保证包含 |
| 轻量 JSON | `localStorage` 中的作品、设定、设置和常用记录 | 模型密钥、来源原件、媒体原件、已归档的记忆历史 |

选择 **恢复备份** 后，Pinax 会先校验文件并预览新增、覆盖、跳过和无法恢复的内容，确认后才写入。恢复可能覆盖当前数据；看清范围，必要时先导出当前完整工作区。

## 数据位置

作品主要保存在当前网址、当前浏览器中，没有自动云同步。协议、主机名或端口变化都会形成不同网页来源；迁移网址前要在旧网址导出备份。无痕窗口、清站点数据、浏览器清理工具、存储配额或设备故障都可能影响内容。
