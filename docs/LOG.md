# 开发日志（精简）

> 本文件只记录近期里程碑、用户可感知变化、验证结果和风险。旧的过程性细节不再保留在主日志中。

## 2026-05-27 - 卡片图片引用与无图态统一

状态：完成首轮，继续收口

结果摘要：
- 画布卡片的图片信息统一成一套轻量引用，导出时只保留素材/附件的轻量元数据，不再塞 base64。
- 无图卡片改成更轻的单行占位信息，和有图卡片保持同一信息节奏，但不增加额外入口。
- 分镜导出的时间轴和提示词现在都能带上图片引用元数据，并会抓取素材最新内容快照，后续接真实导出更直接。

验证：
- `npm run test:run -- src/__tests__/integration.test.js src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线删线命中区与图例收口

状态：完成首轮，继续收口

结果摘要：
- 画布关系线新增透明宽命中区，删线模式下不需要精确点中细线。
- 右上关系线图例折叠态改成图标优先的紧凑工具条，展开/激活时再显示连线文字，减少两个按钮挤在一起的问题。
- 时间轴条目会显示与上一镜已有的关系线类型，并在分镜导出时间轴元数据里保留关系类型与标签。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线删除入口迁回右侧

状态：完成首轮，继续收口

结果摘要：
- 删线入口从左侧节点详情撤回到右侧关系线图例，避免左侧面板继续增厚。
- 右侧图例新增删线模式按钮，进入后可直接点击画布上的关系线删除。
- 选中节点详情恢复为纯节点操作，不再承担关系线列表职责。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线列表可删单线

状态：完成首轮，继续收口

结果摘要：
- 选中节点详情里补了关系线列表，每条线都能直接跳转到关联节点。
- 关系线条目上加了删除按钮，删线动作和当前节点上下文绑定，避免去找画布里的细线。
- 列表项继续维持轻量样式，只在需要时暴露删除入口。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线删除入口接入

状态：完成首轮，继续收口

结果摘要：
- 选中节点详情里新增关系线列表，每条关联都能直接跳转到对应节点。
- 关系线列表里可直接删除单条连线，删除后自动回写并刷新画布。
- 删线入口跟选中节点上下文绑在一起，不额外增加全局面板。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线图例折叠态再平衡

状态：完成首轮，继续收口

结果摘要：
- 右上关系线图例的折叠态重新整理成更稳的两层结构，上面是标题标识，下面是完整宽度的开关。
- 折叠态去掉了过强的装饰感，保留更轻的视觉提示，收起时更像一个小型工具面板而不是临时卡片。
- 展开后的线型列表和连线逻辑没有变化，只调整默认收起态的观感。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线图例继续轻量化

状态：完成首轮，继续收口

结果摘要：
- 右上关系线图例的折叠态进一步去掉厚重的渐变和内阴影，改成更平的轻工具风格。
- 折叠态保留小点标记和单行标题/按钮，视觉权重更低，也更接近一个稳定的小控件。
- 展开状态保持不变，只调整收起态的观感和重量感。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线图例胶囊态美化

状态：完成首轮，继续收口

结果摘要：
- 画布右上关系线图例的折叠态改成更完整的胶囊样式，标题和开关保持同一视觉块，不再像临时排版。
- 折叠态增加轻量点状标记和更柔和的渐变/阴影层次，保持可读性同时让收起状态更像一个正式控件。
- 展开后的线型列表和连线逻辑不变，只调整收起态的观感。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 关系线图例折叠态字体修正

状态：完成首轮，继续收口

结果摘要：
- 画布右上连线图例的折叠态从横排改成上下两行，标题和按钮不再互相挤压。
- 折叠态字体和按钮字号统一收紧，展开后再恢复横排，避免收起时出现不协调的字号和基线。
- 这处调整只针对图例折叠态，不改连线逻辑和线型选择行为。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 画布操作区继续横排化

状态：完成首轮，继续收口

结果摘要：
- 选中节点的操作按钮从上下堆叠改成更紧凑的横排/换行布局，减少左侧详情栏的视觉重量。
- 时间轴条目的景别、运镜和组别信息合并成单行短文本，避免在列表里反复折行。
- 卡片画布继续往“短标题 + 小图 + 少量参数”的方向压缩，信息层级更接近轻量列表。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。
- `npm run build` 通过。

## 2026-05-27 - 画布节点正文预览再收口

状态：完成首轮，继续收口

结果摘要：
- 画布卡片在已有缩略图时不再重复显示正文预览，节点更接近 Git 风格的轻量条目。
- 景别和时长标签统一成同一套轻量小标签，避免不同参数块的字体和视觉权重打架。
- 画布信息密度继续往“短标题 + 小图 + 轻参数”的方向压，减少卡片内部的重复说明。

验证：
- `npm run test:run -- src/__tests__/relationCanvas.test.js` 通过。

## 2026-05-27 - 画布时间轴与连线图例继续收口

状态：完成首轮，继续收口

结果摘要：
- 左侧时间轴已改成竖向紧凑列表，顶部横向预览和底部节奏条都去掉了，镜头顺序只保留一条主线。
- 卡片画布右上角的连线工具收成轻量图例，线型选择和连线开关集中在同一处，不再把说明散到画布里。
- 选中节点详情继续压缩为素材跳转、时间轴切换、镜头参数和删除，正文复述不再占首屏。

验证：
- `npm run verify` 通过。

## 2026-05-27 - 素材侧栏恢复轻量分组标题

状态：完成首轮，继续收口

结果摘要：
- 素材左栏保留了分类分组，但只用轻量标题、点状识别和计数表达层级，不再做厚重边框。
- 素材条目继续保持类型点、状态文本和是否已入画布信号，方便扫读但不堆叠解释。
- 这次调整优先服务“可扫读、可筛选、可直接导入”，不再为了兼容旧侧栏结构保留多余视觉层次。

验证：
- `npm run verify` 通过。

## 2026-05-27 - 工作流重新定位为素材与关系画布

状态：首轮实现完成，继续收口

决策摘要：
- 素材页定位为内容中转层和资产真源，左侧列表已去掉过度装饰和重复解释。
- 原散文卡片页改为通用卡片关系画布，节点以短标题、小预览图、关系线和镜头标记为主；正文与图片完整管理留在素材页。
- 素材已增加“加入/定位卡片画布”动作，卡片优先引用素材，不再另存一套长正文。
- 诗歌独立页面未体现主路径必要性，停止扩展并进入退场计划；低成本可复用的意象扩散动作可迁入卡片画布。

本轮结果：
1. 素材左栏收敛为标题、类型和单一状态信号；素材详情可直接加入/定位卡片画布。
2. 主导航从旧诗歌入口切换到卡片画布，旧诗歌路由仅暂作历史访问入口。
3. 卡片画布节点绑定素材 `assetId`，从素材读取当前内容与缩略图；生成节点会先写入素材。
4. 画布卡片改为紧凑关系节点，仅展示短标题、短预览、缩略图及镜头参数；双击可回素材查看全文。
5. 素材左栏继续收口为按类型折叠的列表，类型颜色与条目状态信号分开表达；正文区删去无效镜头标记和重复实时预览。
6. 关系画布连接已按关系类型提供颜色、粗细、虚实差异和选择预览；现有连接可改类型或断开，并避免同一对节点重复叠线。
7. 素材页工具栏已改为资产操作：类型、处理状态、导当前/导勾选/全导入画布；写作排版、取名、查找等非主路径入口从界面移除。
8. 画布左侧节点详情已去掉正文复述、情绪和情绪扩展；关系连线改为右上图例选线型后直接点两个节点，断线也在画布完成。

下一步：
1. 收口关系画布中的分镜预览和导出。
2. 迁移必要生成动作后删除诗歌路由、页面和专属提示词。

---

## 2026-05-27 - 诗歌节点与散文卡素材接线

状态：完成首轮

结果摘要：
- 诗歌页现在可以把当前意象节点直接送入素材池，沉淀为 `storyboard-seed` 素材，不再只停留在生图预览里。
- 散文页卡片详情里增加了明确的“送入素材”入口，并补了卡片关联面板，卡片之间不再只依赖拖拽碰撞来建立关系。
- 诗歌和散文的分镜提示词已经改成当前工作流语义，重点指向统一分镜、素材池和分镜种子。

用户可感知：
- 诗歌页和散文页都能更直接地把来源内容沉到素材层，再回流到分镜链路里。
- 散文卡片现在既能连线，也能明确转成素材。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 散文镜头卡再收口

状态：完成首轮

结果摘要：
- 散文页卡片上的参考图模块已移除，卡片只保留专业镜头参数展示：景别、运镜、时长、声音。
- 左侧的分镜版本面板已删除，避免和右侧时间轴、顶部导出入口重复。
- 图片相关操作继续留在素材页和生图抽屉，散文卡片只负责消化这些结果，不再承担图片管理职责。

用户可感知：
- 散文分镜页更像制作台，而不是“卡片 + 图片 + 版本”混合面板。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 素材生图与分镜收口

状态：完成首轮

结果摘要：
- 素材页新增生图入口，生成结果可以直接保存为统一素材，也可以一键插入正文，正文里能以 Markdown 图片形式显示。
- 素材左侧列表增加了类型色条、状态标签和更明确的排序，重要素材会更靠前。
- 散文页收掉了卡片/分镜双模式切换，改成单一分镜工作流；卡片上的参考图压缩成预览位，把真正的图片管理放回素材页。
- 散文页现在能直接消耗素材里的参考图和分镜种子，材料和分镜之间的连接更短。

用户可感知：
- 用户在素材页就能看到生图、存图、插图和预览，不再需要先切到别的页面找图片入口。
- 散文页不再有模式切换负担，直接围绕分镜卡和时间轴操作。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 素材参考图接入镜头卡

状态：完成首轮

结果摘要：
- 统一素材服务新增“参考图”素材类型，图片素材可以携带提示词、尺寸、模型和 base64 图片数据。
- 散文页生图预览里的“保存到素材库”现在会真正写入统一素材库，不再只是关闭弹窗。
- 散文页生图抽屉增加“素材参考图”区，选择卡片后可把素材库里的参考图直接挂到当前镜头卡。
- 素材页对参考图素材增加图片预览，避免图片资产只表现成一段文本。

用户可感知：
- 生成图片 -> 存入素材 -> 在分镜镜头卡复用参考图，这条链路已经闭合。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 分镜卡片参考图首轮

状态：完成首轮

结果摘要：
- 散文页分镜态的卡片改为参考图优先展示：每张镜头卡顶部有 16:9 参考图位，有图时大图预览，无图时直接提供“生成参考图”。
- “生成参考图”复用现有生图抽屉和卡片挂图逻辑，会把卡片内容、景别、运镜、时长、声音和情绪整理成镜头提示词。
- 普通卡片态仍保留原有小缩略图，分镜态则把第一张挂图提升为主参考图，减少纯文本分镜的弱引导。

用户可感知：
- 分镜画布里的卡片更像镜头卡，用户可以从卡片直接进入“补参考图 / 换参考图 / 预览参考图”的微调流程。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 分镜画布可视化首轮

状态：完成首轮

结果摘要：
- 诗歌页分镜态在画布里增加了镜头顺序带和节点镜头信息，保留原树状连线逻辑，但视觉上不再与意象态相同。
- 散文页分镜态在画布里增加了顺序带和卡片序号，卡片墙变成更明确的分镜结果视图，而不是原样卡片墙。
- 两页都继续复用原有节点拖拽、连线、分组和时间轴逻辑，只在画布主视觉上追加结果态表达。

用户可感知：
- 分镜态进入后，画布本身就能看出是“结果态”，不会再和原始意象/卡片页面混在一起。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 模式切换门控与画布版本入口

状态：完成首轮

结果摘要：
- 诗歌页和散文页的分镜切换改成门控按钮：没有可用分镜版本时，直接切换会被拦住，先去生成或更新版本。
- 两页都把分镜版本卡片露到来源侧，方便先生成版本，再进入分镜视图；版本卡片显示镜头数、版本号和校验状态。
- 继续复用原有画布逻辑，节点、连线、时间轴和分组仍是主结构，只是在此基础上追加分镜版本态。

用户可感知：
- 用户不会再一键切过去却看到同一批文字还在原地；切换动作会先要求内容准备好。
- 分镜入口从“按钮动作”变成“画布 + 版本卡片”，更接近真正的编辑工作流。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 诗歌与散文分镜版本卡片

状态：完成首轮

结果摘要：
- 诗歌页分镜侧改成版本卡片，显示镜头数、版本号和校验状态；分镜 Markdown 需要先生成版本，再手动下载。
- 散文页分镜侧同样补了版本卡片和独立下载入口，导出菜单里不再把 Markdown 下载伪装成直接导出。
- 模式切换的含义进一步收口为“来源工作流 / 分镜版本”，减少只换标签不换内容的错觉。

用户可感知：
- 用户切到分镜时，先看到版本状态而不是一个立即下载动作，源内容和分镜输出的关系更明确。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 诗歌/散文模式切换收口

状态：完成首轮

结果摘要：
- 诗歌页模式切换从“写作 / 编导”改为“意象 / 分镜”，并使用分段按钮表达工作流选择。
- 散文页模式切换从“写作 / 编导”改为“卡片 / 分镜”，生成按钮、空态和计数文案跟随当前工作流。
- 文档明确后续原则：分镜视图必须生成或选择统一分镜版本，不能只切换标签而复用同一批文本。

用户可感知：
- 用户能更清楚地区分“来源内容整理”和“分镜草稿生成”，模式切换不会再看起来只是刷新标签。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 素材入口、纲要反馈与分镜导出修正

状态：完成首轮

结果摘要：
- 素材从写作页内的弱入口提升为一级导航入口，写作页顶部“素材库”保留为就近入口并强化视觉区分。
- 写作页素材收件箱的“入纲要”改为“加入纲要”，成功后明确提示该素材会参与续写和章节分镜。
- 体验页“生成分镜”不再自动下载文件，改为先生成分镜版本，再在速记面板中手动下载 Markdown。
- 原一级“设定”改名为“世界书”，避免模块内容全是世界书但导航名称过泛。

用户可感知：
- 素材入口更容易发现；章节纲要导入后的用途更明确；分镜导出不再突然触发下载。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 入口顺序与素材页统一收口

状态：完成首轮

结果摘要：
- 一级导航顺序调整为体验、世界书、写作、素材、分镜；体验和写作侧栏的重复切换入口继续收缩。
- 体验页的 AI 开关从界面移除，主题按钮独立展示，设定入口不再塞进“更多”菜单。
- 素材页切换到统一 `narrativeAssets` 资产池，不再走旧笔记列表逻辑；写作页收件箱和素材页现在共用同一批资产。
- 写作页顶部保留“收件箱”处理待办素材，同时新增“素材库”进入完整素材页，避免侧栏切换移除后素材页不可达。
- 首页、欢迎页和执行文档同步改写，减少“笔记”叙事。

用户可感知：
- 体验、世界书、写作、素材、分镜的入口顺序更直接，素材在写作与素材页之间可以互通。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - UI 视口与固定入口首轮收口

状态：完成首轮

结果摘要：
- 新增全局视口高度变量，工作台壳层和体验、写作、素材、分镜相关页面不再直接依赖固定 `100vh`。
- 体验、写作、诗歌和散文页的固定入口改为按可见视口定位，窄屏下按钮纵向堆叠，避免右下角互相覆盖。
- 素材收件箱、体验速记面板等移动端弹层高度改用可见视口计算，降低输入法和横竖屏切换导致的遮挡风险。

用户可感知：
- 小屏和软键盘场景下，编辑区、速记、顾问、生图等入口更可点，不会集中挤在同一个右下角。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 体验与写作顶部入口减负

状态：完成首轮

结果摘要：
- 体验页顶部从一排功能按钮收口为“整理素材 / 分镜 / 会话 / 更多”，AI 开关、主题和设置降级到“更多”。
- 体验页不再重复暴露“世界书导入”按钮，世界书入口交给一级导航和世界书工作区承接。
- 体验页速记面板去掉“保存到素材 / 存为素材”的重复按钮，只保留直接写入素材中台的“保存素材”。
- 写作页顶部移除重复的新建书籍按钮，保留侧栏和空状态里的新建入口；“素材”改为更明确的“收件箱”，“分镜”改为“章节分镜”。

用户可感知：
- 体验和写作页的顶部更像当前任务栏，用户优先看到素材、分镜、会话和收件箱这些主路径，不再被低频设置项挤占。
- 体验速记现在只有一个保存动作，保存后能直接进入写作素材收件箱消费。

验证：
- `npm run verify` 通过。

---

## 2026-05-27 - 产品入口与信息架构收口

状态：完成

结果摘要：
- 一级导航已收口到体验、世界书、写作、素材、分镜。
- 首页和欢迎页入口从“功能页并列”改为“工具链入口分发”。

用户可感知：
- 进入工作台后，主入口理解更直接，不再先暴露体裁分叉。

验证：
- 相关导航与分镜测试通过。
- 构建通过。

---

## 2026-05-27 - 世界书双入口与导入链路增强

状态：完成

结果摘要：
- 世界书能力拆分为普通入口（快速导入）与高级设置（精细编辑）。
- 普通入口支持预设、文本导入、AI 说明生成。
- 同名冲突支持重命名新建、同名新建、覆盖导入。

用户可感知：
- 新手可先导入并快速起盘，进阶用户再进入高级设置细调。

验证：
- 验证通过（测试 + 构建）。
- 冒烟链路可完成导入与冲突处理。

---

## 2026-05-27 - AI 说明生成替换本地随机

状态：完成

结果摘要：
- 说明生成路径已改为 AI 驱动，不再默认本地随机池兜底。
- 缺少 AI 配置时给出显式提示并中止。
- 覆盖冲突前新增条目影响指标（现有/导入/变化量）。

用户可感知：
- “随机生成”变为“AI 生成预览”，结果更贴近输入说明。

验证：
- `npm run verify` 通过。
- 页面冒烟验证通过（无配置提示、本地提炼回退、冲突指标显示）。

---

## 2026-05-27 - UI 重构阶段启动

状态：进行中

当前目标：
1. 优先解决用户可用性问题（核心路径、入口数量、操作层级、任务完成）。
2. 再解决兼容阻塞（视口、滚动、层级、浮层冲突）。
3. 在 UI 基线稳定前暂停新增重功能入口。

已识别风险：
- 页面存在多处固定悬浮入口，交互热区可能重叠。
- 多页面存在 `100vh + overflow + fixed` 叠加策略，移动端易出现遮挡与滚动冲突。
- 页面间断点与布局策略尚未完全统一。

---

## 下一步

1. 先梳理体验、世界书、写作、素材、分镜五条核心路径，移除或降级重复入口。
2. 完成体验页与写作页固定浮层收口，统一层级与关闭行为。
3. 统一视口高度策略，消除移动端软键盘触发布局抖动。
4. 每轮改动后执行核心路径、跨分辨率与主题回归，再执行 `npm run verify`。
